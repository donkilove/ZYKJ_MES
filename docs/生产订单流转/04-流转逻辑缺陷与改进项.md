# 生产订单流转逻辑缺陷与改进项

## 0. 文档定位

- 面向对象：业务、研发、测试共同评审
- 文档目标：基于当前 `01-业务流转总览.md`、`02-研发附录_状态接口与补链.md`、`03-生产订单流转思维导图.md` 三份口径文档，列出**流转逻辑层面**仍然不闭环、不完善或存在歧义的地方
- 口径范围：以仓库 2026-04-26 当前实现为基准
- 处理方式：后续按"一项一议"的节奏逐条澄清、修文档或落代码

---

## 1. 严重程度分级与处理优先级

| 优先级 | 含义 | 包含条目 |
| --- | --- | --- |
| P0 | 直接影响主链正确性，业务上会算错或卡死 | （§2.1、§2.2、§2.6 均已收口） |
| P1 | 影响日常可运营性，会出现挂死或异常状态 | §2.5、§3.7、§3.8、§3.9、§4.10 |
| P2 | 影响可维护性、可追溯性、统一口径 | §5.12、§5.21、§6.14、§6.19 |
| P3 | 长期改善型，先记账、不阻塞当前迭代 | §2.3、§2.4、§5.13、§5.15 |

---

## 2. 主链路逻辑漏洞（优先 P0/P1）

### 2.1 缺陷品对下工序放行量的影响【已澄清，文档已补强（2026-04-27）】

涉及文档：
- `01-业务流转总览.md` §5.4、§6.7、§6.8
- `02-研发附录_状态接口与补链.md` §7、§7.1、§7.2
- `03-生产订单流转思维导图.md` §1（结束生产分支与维修单完成分支）、§3（维修回流流程图）

现状口径（代码已实现，文档已对齐）：
1. `completed_quantity` 定义为良品累计量，结束生产时只加 `quantity`，不加 `defect_quantity`
2. 下工序放行依据 `min(上工序 completed_quantity, 订单数量)`，即只放良品
3. 缺陷件在结束生产时占用本次可生产量但不沉淀进 `completed_quantity`，必须通过维修单完成回流处置：
   - 可修件 → 目标工序 `visible_quantity` 增加（目标工序需 ≤ 来源工序）
   - 跨工序回流 → 来源工序 `visible_quantity` 同步压低
   - 报废且 `scrap_replenished=True` → 首工序 `visible_quantity` 增加
   - 报废且不补料 → 不再回流
4. 订单状态在维修回流路径用 `completed_quantity < visible_quantity` 判定，会把已完成订单拉回 `in_progress`

业务确认结论（2026-04-27）：
- 口径 1（`completed_quantity` 仅含良品）：业务确认，"下一工序的可生产量等于上一工序的良品量"
- 口径 3（缺陷件占用可生产量直到维修完成才放回）：业务确认符合现场预期

衍生待办：详见 §5.21（统一两个订单状态收口函数）

### 2.2 多操作员场景下"每个操作员各自首件"的口径与漏洞【已澄清，文档+代码已对齐（2026-04-28）】

涉及文档：
- `01-业务流转总览.md` §6.4
- `02-研发附录_状态接口与补链.md` §5.3、§6.1

业务确认结论（2026-04-28）：
1. 同一订单、同一工序下，**每个操作员要生产前都必须各自做一次首件检验**——首件是按子工单（操作员）维度做的，不是工序级共享
2. **建单只选工序，不选人**——只要用户当前被绑定到对应工序上，就可以开始自己的首件
3. **订单执行过程中，新增绑定到该工序的操作员也允许开始首件**——不再采用“订单快照锁定参与人”的口径

修复前的逻辑漏洞（已落代码）：
1. 首件门禁要求工序状态 ∈ {pending, partial} 且子工单 pending
2. 首件通过会立即把工序置为 in_progress
3. 工序变 partial 的前置条件是"至少有一笔报工但未达订单总量"
4. A 操作员首件通过 → 工序 in_progress；在 A 报第一笔报工之前工序不会进入 partial → 同道工序的 B 操作员发起首件时，工序仍为 in_progress → 首件门禁直接拒绝
5. 首件通过路径会调用 `_activate_visible_sub_orders` 把同工序所有可见 pending 子工单批量置为 in_progress——既覆盖了"每人各做首件"的口径，又会让没做首件的人误入执行态
6. `ensure_sub_orders_visible_quantity` 会"为后来新增的操作员补 0 数量子工单"，再在分配量提升时变 pending——等价于允许首件后新增操作员

代码层修复要点（worktree `worktree-fix-first-article-per-operator`）：
1. `production_execution_service.submit_first_article`：
   - 工序门禁放开为 `{pending, in_progress, partial}`，让其他操作员也能在工序已进入 in_progress / partial 时各自发起首件
   - 首件通过时**只在工序原本是 `pending` 的情况下**才把工序推进为 `in_progress`，避免把已 partial 的工序倒回 in_progress 丢历史
   - 删除 `_activate_visible_sub_orders`：首件只把当前操作员自己的子工单置为 in_progress，不批量激活其他人
2. `production_execution_service.end_production`：工序门禁放开为 `{in_progress, partial}`——B 操作员首件通过时，工序若已经在 partial，B 也能正常报工
3. `production_execution_service._lock_sub_order`：子工单不存在时允许懒创建——当前只要用户已绑定该工序，就可以在首件时生成参与凭证
4. `production_order_service._backfill_operator_sub_orders` → `_backfill_historical_release_sub_orders`：保留历史放行量回填，但不再承载“分配量快照”职责
5. `production_order_service._build_my_order_item` 与 assist 视角的 `can_first_article / can_end_production` 三处状态集合同步对齐到上述新口径

衍生约束：
- 子工单当前作为“参与凭证 + 执行态载体”存在，不再代表固定分配量

### 2.3 首件不通过后没有重试上限和聚合视图【P3】

涉及文档：`01-业务流转总览.md` §6.4、§6.5

现状只描述"留痕、不放行、操作员重新发起"。缺：
1. 重试次数上限
2. 多次失败时的升级 / 通知机制
3. 是否在订单详情页聚合展示历次失败原因

当前讨论结论（2026-04-28）：
1. **不设重试上限**
2. **不加自动升级 / 通知机制**
3. **不要求订单详情页聚合展示历次失败原因**

收口口径：
- 首件失败继续按“留痕、不放行、操作员重新发起”处理
- 本轮不新增上限、通知或聚合展示能力

### 2.4 首件扫码会话的并发场景缺失【P3】

涉及文档：`02-研发附录_状态接口与补链.md` §6.2

现状：
- 新发起会话只把同操作员同工序下的 `pending / rejected` 改成 cancelled
- `expired` 会话保持原样
- 去重粒度是 `操作员 + 订单工序`，不区分子工单

未覆盖的场景：
1. 操作员在同一工序下被拆出多个子工单时，会话怎么挂
2. expired 会话是否要在新发起时也归档
3. 刷新会话时旧 token 是否立即作废（默认应作废，但没有口径）

当前讨论结论（2026-04-28）：
1. **会话继续按“操作员 + 订单工序”挂载**
   - 不再扩展到子工单维度；共享池预留制下，子工单只是参与凭证，不是分配单元
2. **新发起会话时，旧的 `pending / rejected / expired` 一律归档为 `cancelled`**
   - 同一操作员、同一工序下只保留最新一条可用会话
3. **刷新会话时旧 token 立即作废**
   - 只认最新 token

收口口径：
- 会话去重与归档规则全部收口到“操作员 + 订单工序”这一粒度

### 2.5 代班授权挂死与缺撤销通道【P1，结论已澄清（2026-04-27）】

涉及文档：
- `01-业务流转总览.md` §6.9
- `02-研发附录_状态接口与补链.md` §9

现状口径：
- 创建即生效（approved）
- 仅"结束生产"会 consume
- 同一发起人 + 同一工序 + 同一目标操作员不允许重复挂

漏洞与结论：

| 漏洞 | 结论 |
| --- | --- |
| 1. 代班人只做首件不继续 → 授权永久 approved → 目标操作员无法发起新代班 | **保留现有约束**（同一发起人+工序+目标不得重复），发起人可对不同目标/工序继续发起代班 |
| 2. 没有撤销入口 | **新增撤销代班功能，仅发起人可撤销** |
| 3. 改单时老授权去向未规定 | **改单准入条件：订单所有工序必须是 `pending` 或 `completed`**（即所有工序不在生产中），改单前需先结束生产；老授权按手工完工级联收口逻辑处理（置 consumed） |
| 4. 子工单重新分配后授权量是否合理 | **不再适用**——整体模型从"子工单分配制"切到"共享池预留制"（见 §4.12），子工单不再按操作员固定分配数量

### 2.6 手工完工的副作用收口【已澄清，文档+代码已对齐（2026-04-27）】

涉及文档：
- `01-业务流转总览.md` §6.11
- `02-研发附录_状态接口与补链.md` §4.4

业务确认结论（2026-04-27，1A / 2A / 3A / 4C / 5A 组合）：

1. **首件扫码会话**：把订单下所有仍 `pending` 的会话一律置为 `cancelled`，避免会话依然能被扫码后提交
2. **代班授权**：把 `approved` 但未 consumed 的授权一律置为 `consumed`（沿用既有状态，不新增 cancelled），并写 `consumed_at`
3. **并行实例**：直接复用 `_invalidate_pipeline_instances_for_order(reason="order_completed_manual")`，把仍激活的实例失活
4. **维修单**：维修单不强制收尾，也不允许跟手工完工并存——**当订单还有 `in_repair` 维修单时，手工完工会直接被拒绝（409 Conflict）**，要求先把维修单收掉。这一条是 4C 方案，避免维修回流改 `visible_quantity` 时和手工完工互相打架
5. **事件日志**：仍然只写一条主事件 `order_completed_manual`，但 payload 里把级联收口的实体清单（`cancelled_review_session_ids` / `consumed_assist_authorization_ids` / `invalidated_pipeline_instance_count`）一并写入

代码层修复要点：
1. `production_order_service.complete_order_manually`：
   - 入口前置查 `mes_repair_order` 中 `source_order_id == order.id` 且 `status == in_repair` 的维修单——若存在则抛 `RuntimeError`，由 API 层映射为 409 并把仍在维修的维修单号回带回前端
   - 主链推进逻辑（工序/子工单/订单状态置 completed）保持不变
   - 主链处置后调用新增的 `_cascade_close_order_relations(...)` 一次性完成首件会话取消、代班授权 consumed、并行实例失活
   - 事件日志 payload 新增三类实体清单字段
2. `production_order_service._cascade_close_order_relations`（新增）：
   - 用 `update().where(... in_(ids))` 一次性把会话改 `cancelled`、授权改 `consumed`（顺带写 `consumed_at`）
   - 复用既有 `_invalidate_pipeline_instances_for_order(reason="order_completed_manual")`
   - 返回三类实体的处理结果，由 `complete_order_manually` 写进事件日志

衍生约束（业务侧后续可拍）：
- 维修单本身没有"取消"状态，所以 4C 是"维修必须先走完"。如果后续想支持"订单作废 → 维修单一起作废"，要单独补维修单 cancelled 态（与 §3.7 的订单 cancelled 一起讨论更合理）
- §6.14（事件日志清单）会因为本次 payload 的扩展，增加一组待补充字段——已记入 §6.14

---

## 3. 状态机本身覆盖不完整

### 3.7 订单状态只有 3 个，缺关键业务态【P1】

涉及文档：`01-业务流转总览.md` §5.1

缺失的语义：
- `cancelled / void`：客户取消、临时不做
- `failed / aborted`：工艺路线本身出错且无法回退

当前唯一退出动作只有"手工完工"，被滥用为"取消"会让数据语义彻底失真。

当前讨论结论（2026-04-28）：
- **本轮不新增订单状态**
- `paused`、`cancelled`、`failed` 均不进入当前迭代，维持订单状态 3 态不变

### 3.8 工序状态缺暂停 / 停线语义【P1】

涉及文档：`01-业务流转总览.md` §5.2

现场常见的"换型中、待料、待检"全被压扁到 in_progress / partial，看板和报表无法体现真实瓶颈。

当前讨论结论（2026-04-28）：
- **本轮不新增工序状态**
- `paused / on_hold` 不进入当前迭代
- 若后续要做，应与“换型中 / 待料 / 待检”等原因码一起设计，而不是只新增一个裸状态

### 3.9 订单一旦开工不可改过于刚性【P1】

涉及文档：
- `01-业务流转总览.md` §6.2
- `02-研发附录_状态接口与补链.md` §4.2、§4.3

现状：进入 in_progress 后路线、操作员、并行模式都没有合法修改通道。
现场常见诉求：
- 调换班组
- 追加 / 减少数量
- 换工艺步骤

当前只能"手工完工 + 重开新单"，链路被强行切断。

当前讨论结论（2026-04-28）：
- 改单继续保留，但分为三档：
1. **允许直接修改**：备注、交期、非主链展示信息
2. **条件允许修改**：操作员范围、数量、工艺路线
   - 前置条件：订单下所有工序都不在生产中，即全部为 `pending` 或 `completed`
3. **不允许修改**：历史报工记录、历史维修单归属、历史首件记录、历史事件日志

收口口径：
- 改单只改“未来如何继续做”，不回写或篡改已经发生的历史数据
- 与 §2.5 的改单准入保持一致：先停手，再改单

---

## 4. 并行模式悬空场景

### 4.10 流水线模式门禁规则未写【P1，结论已澄清（2026-04-28，后续口径已收正）】

涉及文档：
- `01-业务流转总览.md` §6.10
- `02-研发附录_状态接口与补链.md` §10

业务确认结论（2026-04-28）：
1. **下游工序不能先于上游开始首件**
   - 同一流水线序号下，下一工序开始首件前，上一工序对应流水线实例必须已经先完成首件通过并建立链路实例
2. **同一流水线序号跨工序可以串行推进**
   - `seq=1` 在工序 A、B、C 上形成连续链路；B 的 `seq=1` 只能承接 A 的 `seq=1`
3. **各实例之间的数量关系仍走共享池**
   - 不再给单条流水线实例切固定分配量；流水线实例只负责顺序门禁与链路追踪，数量仍按共享池预留制计算

实现含义：
- 门禁判断由“实例链是否已在上游建立并通过首件”决定，不由实例级 assigned_quantity 决定
- 流水线模式与共享池预留制并存，不回退到子工单分配制

### 4.11 流水线模式中途变更没有过渡口径【P1，结论已澄清（2026-04-28）】

涉及文档：
- `01-业务流转总览.md` §6.10
- `02-研发附录_状态接口与补链.md` §10

业务确认结论（2026-04-28）：
1. **已失效实例的历史报工数量保留在主链，不回滚**
   - 流水线实例只负责链路追踪与门禁，不承担数量账本职责；已写入 `ProductionRecord` 与工序 `completed_quantity` 的数量继续有效
2. **失效后再启用时，历史 `pipeline_instance_id` 保留原值，不回写**
   - 新启用后创建新实例，后续记录挂新实例 id；历史记录保留当时真实发生的实例绑定
3. **若仍有未完成工序，不允许关闭流水线模式**
   - 关闭流水线模式的前置条件是相关工序全部完成；避免半途切模式导致门禁、追踪链和现场执行口径混乱

实现含义：
- 流水线模式关闭不再提供“执行中切回普通模式”的过渡态
- 模式切换只允许在完整收口后执行，避免与共享池预留制混用产生歧义

### 4.12 整体模型从"子工单分配制"切到"共享池预留制"【P0，代码已基本实现（2026-04-28）】

涉及文档：§2.5 讨论衍生，详见 `docs/superpowers/specs/2026-04-27-共享池预留制-design.md`

核心变更：
- 子工单去掉 `assigned_quantity`、`is_visible`，保留表结构作为参与凭证；`done` 仅保留给手工完工收口场景
- 工序 `visible_quantity` 由所有操作员共享，首件通过预留 1 件
- 子工单懒创建，在操作员第一次点首件时生成
- 并行实例改名"流水线模式"，主链已切到工序级懒创建实例
- 维修回流去掉均分到子工单步骤
- 我的工单展示改为工序级（不依赖子工单）

---

## 5. 查询补链与再分配

### 5.12 visible_quantity 回填只挂在查询入口【P2】

涉及文档：`02-研发附录_状态接口与补链.md` §8.2

现状：
- "我的工单 / 代理工单" 查询前会做 `_backfill_historical_release_quantity`
- "我的代班工单" 不做
- 管理端列表、报表导出、数据迁移、定时任务等完全绕过补链

风险：
- 补链是"兜底"而不是"机制"
- 长尾下数据会越积越歪

建议：
- 把补链下沉成领域事件
- "工序累计完成量变化"或"工序操作员变化"时同步触发，而不是查询触发

当前讨论结论（2026-04-28）：
1. **子工单层补链退出历史**
   - 共享池预留制下，子工单不再承载 `assigned_quantity` 分配逻辑，因此不再需要子工单层补链
2. **工序级 `visible_quantity` 查询兜底暂时保留**
   - 作为历史数据与长尾分支的安全网，当前迭代允许查询入口继续兜底修正工序级可见量
3. **长期目标是写时收口，逐步移除查询补链**
   - 后续应把 `visible_quantity` 的同步下沉到正常写路径（报工、维修回流、补料回流、改单等），做到“写时正确，查时只读”

收口口径：
- 本轮不再修子工单补链问题
- 工序级补链暂时容忍查询兜底，但不再视为长期机制

### 5.13 操作员变更没有再分配的合法动作【P3】

涉及文档：`02-研发附录_状态接口与补链.md` §8.1

现状：
- 新增操作员只补 0 数量子工单
- 没有"重新均分 / 主动调拨"的接口
- 离职 / 调岗操作员名下未完成的子工单没有合法搬移路径

后果：
- 新员工长期处于"零可见量子工单"的僵尸状态
- 老员工挂着无法消化的存量

当前讨论结论（2026-04-28）：
1. **新增操作员不再补 0 数量子工单**
   - 共享池预留制下，只要操作员具备对应工序角色/能力，后续即可在该工序首件；第一次首件时懒创建子工单
2. **移除操作员按是否在产区分处理**
   - 若该操作员当前没有 `in_progress` 子工单，可直接移出工序参与范围
   - 若该操作员当前仍在 `in_progress`，不允许直接移除，必须先结束生产
3. **不再提供“再分配数量”接口**
   - 共享池模式下不存在 `assigned_quantity`，因此不存在“把数量从 A 调拨到 B”的动作；只维护“谁有资格参与该工序”

收口口径：
- 该条从“再分配问题”转为“参与范围维护问题”
- 不回退到子工单分配制

### 5.21 订单状态收口函数：普通报工 vs 维修完成的隐性不一致【P2】

涉及代码：
- `production_execution_service.py::_refresh_order_status`
- `production_repair_service.py::_recompute_order_status_by_quantities`

现状：
1. 普通报工流程在结尾调 `_refresh_order_status`，判定标准是"所有工序 `status == COMPLETED`"
2. 维修单完成流程在结尾调 `_recompute_order_status_by_quantities`，判定标准是"所有工序 `completed_quantity ≥ visible_quantity`"
3. 两条路径在普通现场结果一致，但属于隐性不一致

风险：
- 任一边动一下（例如改 `process.status` 状态机或 `visible_quantity` 赋值时机）都可能让两条路径分叉
- 维修回流后再下一次普通报工触发 `_refresh_order_status` 时，可能把维修流程刚刚标过的订单状态再覆盖回来

建议：
- 收口为单一函数（推荐数量比较版："所有工序 `completed_quantity ≥ visible_quantity` 视为完成"），普通报工流程也调用
- 增加单测断言：报工 → 维修完成 → 报工 在三阶段订单状态都正确

来源：§2.1 衍生

---

## 6. 可追溯与审计

### 6.14 订单事件日志没有完整事件清单【P2】

涉及文档：全部三篇

现状：文档反复出现"写订单事件日志 / 写事件留痕"，但没有一处枚举完整事件类型。

至少应补充对照表：
- 建单 / 改单 / 删单 / 手工完工
- 首件通过 / 首件不通过 / 首件扫码会话状态
- 报工完成 / 缺陷自动建维修单
- 下工序放行（含补链触发）
- 代班授权创建 / 消耗 / 撤销
- 并行模式变更 / 实例失效 / 实例激活

否则审计排障没有抓手。

当前讨论结论（2026-04-28）：
先收口一版**最小闭环事件清单**：

1. 订单层
   - `order_created`
   - `order_updated`
   - `order_deleted`
   - `order_completed_manual`
2. 首件层
   - `first_article_passed`
   - `first_article_failed`
   - `first_article_review_session_created`
   - `first_article_review_session_cancelled`
   - `first_article_review_session_expired`
3. 生产执行层
   - `production_reported`
   - `defect_repair_order_created`
   - `process_visible_quantity_released`
4. 代班层
   - `assist_authorization_created`
   - `assist_authorization_cancelled`
   - `assist_authorization_consumed`
5. 流水线层
   - `pipeline_instances_invalidated`
   - `pipeline_instances_activated`
6. 维修层
   - `repair_order_completed`

收口口径：
- 先保证主链排障可用，再按需要扩细粒度事件

### 6.15 缺批次 / 序列号粒度【P3】

涉及文档：`01-业务流转总览.md` §4、`02-研发附录_状态接口与补链.md` §7

现状：`ProductionRecord` 只到数量级别。

行业现实：
- 化工 / 食品 / 医疗器械 / 关键件加工要求批号或序列号级追溯
- 当前模型缺乏对批次 / SN 的扩展点

当前讨论结论（2026-04-28）：
- **当前生产订单主链只做到数量级追溯**
- 本轮不引入批次号 / SN 级追溯
- 若后续要做，必须作为独立能力建设推进，不能只给 `ProductionRecord` 零散补字段

至少会牵到：
1. 报工录入模型
2. 缺陷 / 维修绑定粒度
3. 回流与报废归属
4. 查询与导出
5. 可能的扫码链路

收口口径：
- 批次 / SN 追溯不进入当前主链迭代，先维持数量级追溯

---

## 7. 其它实操层面的不完善

### 7.16 工艺模板与运行中订单的解耦关系没写【P2】

涉及文档：`02-研发附录_状态接口与补链.md` §4.1

未明示：模板被改了，已经 in_progress 的订单是否要同步？默认应该是"不同步"，但需要在文档里说出来。

当前讨论结论（2026-04-28）：
- **工艺模板修改后，不自动影响已经创建出来的订单**
- 订单在创建时就把工艺路线固化为自己的工序快照
- 若后续希望订单吃到新模板，只能通过**改单**显式重建路线，不允许模板变更自动推送到运行中订单

收口口径：
- 订单按“创建时快照”执行，不按“模板最新态”执行

### 7.17 数量量纲与精度未声明【P3】

涉及文档：全部三篇

现状只用"数量"措辞，没说：
- 量纲（件 / 公斤 / 米）
- 是否允许小数
- 比较与累加时的精度策略

这会反向影响 `min`、累加、可生产量等核心计算的语义。

当前讨论结论（2026-04-28）：
- **当前生产订单主链数量单位默认按“件”处理**
- **当前迭代只支持整数**
- 所有比较、累加、放行、回流都按整数处理
- 本轮不讨论公斤 / 米 / 小数精度

收口口径：
- 若后续要支持非“件”单位或小数精度，需要把整条主链升级为“数量 + 单位 + 精度策略”模型，不能只改单点字段

### 7.18 权限网关偏弱【P2】

涉及文档：
- `01-业务流转总览.md` §6.11
- `02-研发附录_状态接口与补链.md` §4.4

现状：
- 手工完工只校验"当前登录密码"
- 没有"角色 / 审批 / 双人复核"维度
- 删除待生产订单也没有二次确认

当前讨论结论（2026-04-28）：
- **沿用现有能力包控制手工完工，不额外新增双人复核或审批流**
- 当前仓库已存在权限能力 `production.orders.complete`（后端常量 `PERM_PROD_ORDERS_COMPLETE`，前端能力模型同步存在）
- 后续由业务侧手动把该能力分配给指定角色/用户即可

收口口径：
- 手工完工的门禁优先依赖现有权限能力包控制
- 本轮不新增额外审批链

### 6.19 维修闭环被踢出范围，但报工又会自动建维修单【P2】

涉及文档：
- `01-业务流转总览.md` §2、§6.8
- `02-研发附录_状态接口与补链.md` §7.2

矛盾点：
- §2 把"维修单后续维修闭环"排除在文档之外
- §6.8 又把"自动建维修单"放在主链路硬动作里

读者拿到这份文档无法看完整产线。建议至少补一节"维修单与生产订单的回写边界"。

当前讨论结论（2026-04-28）：
- **维修子系统正式纳入生产订单主文档范围**
- 生产订单文档必须覆盖从“报工产生缺陷”到“维修完成回写生产订单”的完整闭环
- 不能只写“自动建维修单”这一入口动作

至少纳入以下四块：
1. 维修单状态机
2. 维修单回流规则
3. 报废 / 补料规则
4. 维修单对生产订单的回写规则

收口口径：
- 后续三份主文档都应把维修子系统视为主链组成部分，而不是外挂说明

### 7.20 思维导图与正文小不一致【P3】

涉及文档：
- `03-生产订单流转思维导图.md` §1（并行模式约束）
- `01-业务流转总览.md` §6.10

不一致点：
- 思维导图：至少两个有效工序编码
- 正文：工序总数 ≥ 2 且被选并行工序 ≥ 2

需要统一表达，避免业务侧按思维导图看口径变松。

当前讨论结论（2026-04-28）：
- 统一采用**正文**口径：
  - 订单工序总数必须 `>= 2`
  - 被选为流水线模式的工序数量必须 `>= 2`

收口口径：
- 思维导图后续应改成与正文一致，不再使用“至少两个有效工序编码”的宽口径表达

---

## 8. 处理跟踪表（逐条解决用）

| 编号 | 标题 | 优先级 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| 2.1 | 缺陷品对下工序放行量的影响 | P0 | 已澄清，文档已补强（2026-04-27） | 衍生 §5.21 |
| 2.2 | 多操作员每人各自首件 + 工序绑定即允许首件 | P0 | 已澄清，文档+代码已对齐（2026-04-28） | 建单只选工序，不锁定参与人 |
| 2.3 | 首件不通过的重试上限与聚合 | P3 | 结论已澄清（2026-04-28） | 无上限、无通知、无详情聚合 |
| 2.4 | 首件扫码会话的并发场景 | P3 | 结论已澄清（2026-04-28） | 会话粒度=操作员+工序；旧 pending/rejected/expired 一律 cancel；旧 token 立即失效 |
| 2.5 | 代班授权挂死与撤销通道 | P1 | 已实现（2026-04-28） | 1)保留现有重复约束 2)新增撤销(仅发起人) 3)改单准入=pending/completed 4)共享池预留制兜底 |
| 2.6 | 手工完工的副作用收口 | P0 | 已澄清，文档+代码已对齐（2026-04-27） | 1A/2A/3A/4C/5A |
| 3.7 | 订单状态缺 cancelled / paused / failed | P1 | 已讨论，暂不进入本迭代（2026-04-28） | 维持 3 态不变 |
| 3.8 | 工序状态缺暂停 / 停线 | P1 | 已讨论，暂不进入本迭代（2026-04-28） | 后续应连原因码一起设计 |
| 3.9 | 已开工订单不可改过于刚性 | P1 | 结论已澄清（2026-04-28） | 直接改备注/交期；停手后可改操作员/数量/路线；历史数据不可回写 |
| 4.10 | 流水线模式门禁规则未写 | P1 | 结论已澄清（2026-04-28） | 下游不能先于上游首件通过；同序号跨工序串行推进；数量仍走共享池 |
| 4.11 | 流水线模式中途变更没有过渡口径 | P1 | 结论已澄清（2026-04-28） | 历史数量保留；历史实例ID不回写；未完工序不允许关流水线模式 |
| 4.12 | 整体模型切到共享池预留制 | P0 | 基本实现（2026-04-28） | 含流水线改名 `ProcessPipelineInstance` 与工序级懒创建实例 |
| 5.12 | 补链只挂在查询入口 | P2 | 结论已澄清（2026-04-28） | 子工单补链退出；工序级查询兜底暂留；长期改写时收口 |
| 5.13 | 操作员变更没有再分配动作 | P3 | 结论已澄清（2026-04-28） | 转为“参与范围维护”；不再做数量再分配 |
| 5.21 | 统一两个订单状态收口函数 | P2 | 已实现（2026-04-28） | `_refresh_order_status` 数量比较版；`_recompute_order_status_by_quantities` 委托 |
| 6.14 | 事件日志没有完整清单 | P2 | 结论已澄清（2026-04-28） | 先收最小闭环事件清单 |
| 6.15 | 缺批次 / 序列号粒度 | P3 | 结论已澄清（2026-04-28） | 本轮只做数量级追溯；批次/SN 另立能力建设 |
| 7.16 | 工艺模板与运行中订单解耦关系未写 | P2 | 结论已澄清（2026-04-28） | 模板改动不自动影响已创建订单；订单按快照执行 |
| 7.17 | 数量量纲与精度未声明 | P3 | 结论已澄清（2026-04-28） | 默认单位“件”；仅支持整数 |
| 7.18 | 权限网关偏弱 | P2 | 结论已澄清（2026-04-28） | 手工完工沿用 `production.orders.complete` 能力包控制 |
| 6.19 | 维修闭环边界缺口 | P2 | 结论已澄清（2026-04-28） | 维修子系统纳入生产订单主文档范围 |
| 7.20 | 思维导图与正文小不一致 | P3 | 结论已澄清（2026-04-28） | 统一采用正文口径：工序总数>=2且被选流水线工序>=2 |

---

## 9. 推荐解决顺序

1. ~~§2.1 缺陷品对下工序放行量的影响~~ —— 已澄清，文档已补强（2026-04-27），衍生出 §5.21 待办
2. ~~§2.2 多操作员每人各自首件 + 禁止首件后新增操作员~~ —— 已澄清，文档+代码已对齐（2026-04-27）
3. ~~§2.6 手工完工副作用收口~~ —— 已澄清，文档+代码已对齐（2026-04-27），按 1A/2A/3A/4C/5A 组合落实
4. 接下来处理 §2.5、§3.7~§3.9、§4.10、§4.11 这些 P1 可运营性问题
5. 最后清扫 P2 / P3 的口径与扩展性（含新增 §5.21）
