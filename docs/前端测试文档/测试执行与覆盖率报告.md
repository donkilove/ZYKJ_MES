# 前端测试执行与覆盖率报告

## 1. 执行日期
- 2026-03-06

## 2. 执行环境
- Flutter: `3.41.2`
- Dart: `3.11.0`
- 执行目录：`mes_client`

## 3. 执行命令

```powershell
flutter test
flutter test --coverage
```

覆盖率计算命令：

```powershell
$lf=0; $lh=0; Get-Content coverage/lcov.info | ForEach-Object {
  if ($_ -match '^LF:(\d+)$') { $lf += [int]$Matches[1] }
  elseif ($_ -match '^LH:(\d+)$') { $lh += [int]$Matches[1] }
}
$pct = [math]::Round(($lh * 100.0 / $lf), 2)
"Coverage: $pct% ($lh/$lf)"
```

## 4. 结果
- 测试结果：全部通过（53 个测试用例）
- 覆盖率结果：`86.78% (1601/1845)`
- 结论：达到并超过目标覆盖率 `80%`

## 5. 主要覆盖文件
- 模型层：`mes_client/lib/models/*`
- 服务层：`mes_client/lib/services/*`
- 通用组件：`mes_client/lib/widgets/*`

## 6. 产物路径
- 测试代码：`mes_client/test`
- 覆盖率报告：`mes_client/coverage/lcov.info`
