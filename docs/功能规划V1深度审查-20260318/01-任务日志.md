# 任务日志

## 1. 任务信息

- 任务名称：对照功能规划 V1 深度检查前后端实现
- 执行日期：2026-03-18
- 执行方式：静态代码审查
- 代码改动策略：不改业务代码，仅新增检查文档

## 2. 输入来源

- 用户指令：对照 `功能规划文件夹V1` 中各模块需求说明，深度检查后端与前端，不直接改动代码，并将结果放入 `docs` 子目录。
- 需求文档来源：`docs/功能规划V1/`
- 代码来源：仓库 `backend/`、`frontend/`

## 3. 执行步骤

1. 盘点仓库目录，确认需求文档位于 `docs/功能规划V1/`，确认前端位于 `frontend/`、后端位于 `backend/`。
2. 逐模块对照需求文档、前端页面与服务、后端接口与服务、相关模型与 Schema。
3. 汇总每个模块的“已满足 / 部分满足 / 未满足 / 明显偏差 / 关键证据”。
4. 将结果沉淀到 `docs/功能规划V1深度审查-20260318/`。

## 4. 审查限制与假设

- 本次审查未执行接口联调、前端构建、自动化测试与人工页面点击验证。
- 结论以当前仓库可见代码为准；若存在未提交本地改动、外部配置或运行时注入逻辑，本报告未覆盖。
- 文中证据以代码文件与行号为准，后续若代码继续变更，行号可能发生偏移。

## 5. 输出文件

- `docs/功能规划V1深度审查-20260318/00-总览.md`
- `docs/功能规划V1深度审查-20260318/10-用户模块.md`
- `docs/功能规划V1深度审查-20260318/11-产品模块.md`
- `docs/功能规划V1深度审查-20260318/12-工艺模块.md`
- `docs/功能规划V1深度审查-20260318/13-设备模块.md`
- `docs/功能规划V1深度审查-20260318/14-品质模块.md`
- `docs/功能规划V1深度审查-20260318/15-生产模块.md`
- `docs/功能规划V1深度审查-20260318/16-消息模块.md`

## 6. 交付说明

- 本次交付无迁移，直接新增审查文档。
- 本次交付不包含整改代码，不包含补测结果，不包含提交 Git commit。

## 7. 二次跟进：用户模块整改细化

- 跟进日期：2026-03-18
- 跟进目标：在用户模块审查结论基础上，继续细化到页面、接口、数据表三级改造点，便于进入设计与实施阶段。
- 产出文件：`docs/功能规划V1深度审查-20260318/20-用户模块整改细化.md`
- 跟进结论：用户模块优先整改应聚焦 3 个 P0 项：
  1. 自定义角色可建不可分配；
  2. 系统管理员权限配置保底缺失；
  3. 用户模块对外契约仍夹带 `process_codes`，与需求“角色 + 工段”模型不一致。

## 8. 三次跟进：用户模块批次 A 已实施

- 跟进日期：2026-03-18
- 实施范围：批次 A（U-01、U-02、U-03）
- 已落地内容：
  - 后端用户/审批公开契约从 `role_codes` 收敛为 `role_code`，并移除 `process_codes` 输入；
  - 用户模块前端新建、编辑、审批通过弹窗同步收敛为“单角色 + 单工段”；
  - 新增 `sys_user_role.user_id` 唯一约束迁移，强制单角色绑定；
  - 权限配置保存与系统管理员停用/删号路径新增“权限配置入口保底”校验；
  - 功能权限配置页对系统管理员的系统模块保底能力增加前端锁定提示。
- 关键文件：
  - `backend/app/services/user_service.py`
  - `backend/app/services/authz_service.py`
  - `backend/app/api/v1/endpoints/users.py`
  - `backend/app/api/v1/endpoints/auth.py`
  - `frontend/lib/pages/user_management_page.dart`
  - `frontend/lib/pages/registration_approval_page.dart`
  - `frontend/lib/pages/function_permission_config_page.dart`
  - `backend/alembic/versions/k3l4m5n6o7p8_enforce_single_role_per_user.py`
- 验证结果：
  - 已执行 `python -m compileall backend/app backend/alembic`，通过；
  - 已执行 `flutter analyze` 针对本次改动文件检查，结果通过。

## 9. 四次跟进：已执行迁移并实施批次 B

- 跟进日期：2026-03-18
- 已执行迁移：`C:\Users\Donki\UserData\Code\ZYKJ_MES\.venv\Scripts\alembic.exe upgrade head`
- 迁移结果：成功执行 `j2k3l4m5n6o7 -> k3l4m5n6o7p8`
- 已实施范围：
  - U-04：将用户模块能力包从粗粒度拆分为细粒度功能项，包括用户创建、编辑、启停、重置密码、删除、导出，注册审批查看/通过/驳回，角色查看/新建/编辑/启停/删除，个人资料/改密/当前会话，登录日志/在线会话/强制下线；
  - U-05：注册审批页增加关键字搜索与状态筛选，支持查看已通过、已驳回历史；
  - U-06：用户导出接口与前端导出动作补齐 `is_online` 过滤，导出结果与当前筛选保持一致。
- 关键文件：
  - `backend/app/core/authz_hierarchy_catalog.py`
  - `backend/app/api/v1/endpoints/users.py`
  - `frontend/lib/pages/registration_approval_page.dart`
  - `frontend/lib/pages/user_page.dart`
  - `frontend/lib/models/authz_models.dart`
  - `frontend/lib/services/user_service.dart`
- 验证结果：
  - 已再次执行 `python -m compileall backend/app backend/alembic`，通过；
  - 已执行 `flutter analyze` 针对用户模块相关改动文件检查，结果通过。

## 10. 五次跟进：用户模块批次 C 已实施

- 跟进日期：2026-03-18
- 已实施范围：
  - 角色管理页从卡片列表改为表格，补齐“角色名称、角色说明、角色类型、关联用户数、状态、操作”；
  - 登录日志页与在线会话页改为表格展示；在线会话状态文案已显式映射为“在线 / 已强制下线 / 已过期”；
  - 个人中心页移除了会话令牌展示；
  - 用户删除确认文案已改为符合“逻辑删除 + 停用 + 隐藏常规列表”的真实语义。
- 关键文件：
  - `frontend/lib/pages/role_management_page.dart`
  - `frontend/lib/pages/login_session_page.dart`
  - `frontend/lib/pages/account_settings_page.dart`
  - `frontend/lib/pages/user_management_page.dart`
  - `backend/app/core/authz_catalog.py`
  - `backend/app/services/authz_service.py`
- 验证结果：
  - 已执行 `python -m compileall backend/app backend/alembic`，通过；
  - 已执行 `flutter analyze` 针对批次 C 相关前端文件检查，结果通过。

## 11. 六次跟进：前端测试同步修复

- 跟进日期：2026-03-18
- 背景：完整执行 `flutter analyze` 后发现报错集中在 `frontend/test/`，业务代码 `frontend/lib/` 本身无分析错误，问题由测试代码未同步用户模块“单角色 + 单工段”契约导致。
- 已修复文件：
  - `frontend/test/models/user_models_test.dart`
  - `frontend/test/services/user_service_test.dart`
  - `frontend/test/widgets/user_management_page_test.dart`
- 修复内容：
  - 将测试模型、Mock JSON、断言从 `roleCodes/processCodes/stageNames` 更新为 `roleCode/roleName/stageName`；
  - 将 `UserService` 相关测试调用与请求体断言同步到 `role_code` 新契约；
  - 将 `FakeUserService` override 签名与 `UserItem` 构造参数同步到当前实现；
  - 将依赖“工序反推工段”的过时用例收敛为“未配置工段时提示选择工段”的当前行为校验。
- 验证结果：
  - 已执行 `flutter analyze`，结果 `No issues found!`；
  - 已执行 `flutter test test/models/user_models_test.dart test/services/user_service_test.dart test/widgets/user_management_page_test.dart`，全部通过。

## 12. 七次跟进：统一 3 个用户读接口契约

- 跟进日期：2026-03-18
- 统一方案：
  - `/auth/me`：从 `role_codes/role_names/process_codes/process_names` 收敛为 `role_code/role_name/stage_id/stage_name`；
  - `/me/profile`：从 `role_codes/role_names/stage_name` 收敛为 `role_code/role_name/stage_id/stage_name`；
  - `/sessions/online`：从 `role_codes/role_names/stage_name` 收敛为 `role_code/role_name/stage_id/stage_name`；
  - 统一目标：用户读接口全部采用“单角色 + 单工段”公开契约，不再公开 `process_codes/process_names`。
- 已落地文件：
  - 后端：`backend/app/schemas/auth.py`、`backend/app/api/v1/endpoints/auth.py`、`backend/app/schemas/me.py`、`backend/app/api/v1/endpoints/me.py`、`backend/app/schemas/session.py`、`backend/app/api/v1/endpoints/sessions.py`
  - 前端：`frontend/lib/models/current_user.dart`、`frontend/lib/models/user_models.dart`、`frontend/lib/pages/user_management_page.dart`、`frontend/lib/pages/account_settings_page.dart`、`frontend/lib/pages/home_page.dart`、`frontend/lib/pages/login_session_page.dart`
  - 测试：`frontend/test/models/current_user_test.dart`、`frontend/test/services/auth_service_test.dart`、`frontend/test/widgets/user_management_page_test.dart`
- 验证结果：
  - 已执行 `python -m compileall backend/app`，通过；
  - 已执行 `flutter analyze`，通过；
  - 已执行 `flutter test test/models/current_user_test.dart test/services/auth_service_test.dart test/models/user_models_test.dart test/services/user_service_test.dart test/widgets/user_management_page_test.dart`，全部通过。

## 13. 八次跟进：用户模块读接口联调验证

- 跟进日期：2026-03-18
- 联调方式：
  - 通过 `start_backend.py --no-reload --port 8001` 启动本地后端；
  - 使用默认管理员账号 `admin / Admin@123456` 登录；
  - 依次调用 `/api/v1/auth/me`、`/api/v1/me/profile`、`/api/v1/sessions/online?page=1&page_size=5` 验证真实返回。
- 联调结果：
  - 登录成功，返回 `access_token`、`token_type`、`expires_in`、`must_change_password=false`；
  - `/auth/me` 已返回 `role_code`、`role_name`、`stage_id`、`stage_name` 单值结构；
  - `/me/profile` 已返回 `role_code`、`role_name`、`stage_id`、`stage_name` 单值结构，且附带账号状态、创建时间、最近登录时间等信息；
  - `/sessions/online` 已返回 `role_code`、`role_name`、`stage_id`、`stage_name` 单值结构，在线会话查询成功。
- 备注：
  - 命令行输出中的中文角色名一度出现乱码，经 `ensure_ascii=True` 复核后确认接口内容实际正确，为终端编码显示问题，不是接口编码问题；
  - 联调结束后已停止本地 8001 端口后端进程。

## 14. 九次跟进：产品模块分批次整改计划

- 跟进日期：2026-03-18
- 背景：用户模块已完成本轮整改，下一阶段转入产品模块；本次仅输出分批次计划，不改动产品模块业务代码。
- 产出文件：`docs/功能规划V1深度审查-20260318/21-产品模块整改细化.md`
- 计划结论：
  - 产品模块必须先做“版本参数主模型重构”，否则后续版本、生效、查询、导出、追溯整改都会继续建立在错误数据模型上；
  - 建议分 4 个批次推进：
    1. 批次 A：版本参数主模型与公开契约替换；
    2. 批次 B：版本生命周期与并发生效规则闭环；
    3. 批次 C：查询、导出、详情、历史追溯一致性收口；
    4. 批次 D：清理旧表与补齐联调/回归验证。

## 15. 十次跟进：产品模块批次 A 已实施

- 跟进日期：2026-03-18
- 已实施范围：
  - 新增 `mes_product_revision_parameter` 版本参数主表，并通过迁移从历史版本快照回填版本参数；
  - `mes_product_parameter_history` 补 `revision_id`、`version`，使参数历史可与版本关联；
  - 后端新增版本参数查询、版本参数更新、生效版本参数查询、版本参数历史查询能力；
  - 前端产品参数管理页改为按当前版本读写，产品参数查询页和产品详情参数区改为按生效版本只读查询；
  - 前端产品模型、服务层与测试已同步到版本参数新契约。
- 关键文件：
  - `backend/app/models/product_revision_parameter.py`
  - `backend/alembic/versions/m4n5o6p7q8r9_add_product_revision_parameter_table.py`
  - `backend/app/services/product_service.py`
  - `backend/app/api/v1/endpoints/products.py`
  - `backend/app/schemas/product.py`
  - `frontend/lib/models/product_models.dart`
  - `frontend/lib/services/product_service.dart`
  - `frontend/lib/pages/product_parameter_management_page.dart`
  - `frontend/lib/pages/product_parameter_query_page.dart`
  - `frontend/lib/pages/product_management_page.dart`
- 验证结果：
  - 已执行 `python -m compileall backend/app backend/alembic`，通过；
  - 已执行 `flutter analyze`，通过；
  - 已执行 `flutter test test/models/product_models_test.dart test/services/product_service_test.dart test/widgets/product_module_issue_regression_test.dart`，全部通过；
  - 已执行 `alembic upgrade head`，迁移成功。
- 联调备注：
  - 已启动本地后端并使用管理员账号验证产品列表接口；
  - 当前数据库中无产品数据，无法继续对版本参数读接口做真实数据联调；
  - 因避免污染现有环境，本轮未主动创建联调用产品数据。

## 16. 十一次跟进：产品模块批次 B 核心链路已实施

- 跟进日期：2026-03-18
- 已实施范围：
  - 复制版本不再覆盖 `mes_product_parameter` 当前参数表，改为直接复制来源版本参数到新草稿版本参数表；
  - 版本立即生效新增 `expected_effective_version` 并发保护，避免抢先生效静默覆盖；
  - 版本生效时同步刷新当前参数表到目标版本参数，确保当前版本语义与版本状态一致；
  - 删除草稿版本时，会回落 `current_version` 并同步当前参数表；
  - 前端版本管理页激活动作会携带期望生效版本；复制/激活/停用/删除后会刷新选中产品与版本列表；
  - 版本管理页“维护参数”已打通到参数管理页，非草稿版本会按只读模式打开。
- 关键文件：
  - `backend/app/services/product_service.py`
  - `backend/app/api/v1/endpoints/products.py`
  - `backend/app/schemas/product.py`
  - `frontend/lib/services/product_service.dart`
  - `frontend/lib/pages/product_version_management_page.dart`
  - `frontend/lib/pages/product_parameter_management_page.dart`
  - `frontend/lib/pages/product_page.dart`
  - `frontend/lib/models/product_models.dart`
- 验证结果：
  - 已执行 `python -m compileall backend/app`，通过；
  - 已执行 `flutter analyze`，通过；
  - 已执行 `flutter test test/models/product_models_test.dart test/services/product_service_test.dart test/widgets/product_module_issue_regression_test.dart`，全部通过；
  - 已通过真实接口联调验证：
    1. V1.0 生效后参数值为 `CHIP-A`；
    2. 复制 V1.1 并修改为 `CHIP-B` 后，生效版本仍保持 `CHIP-A`；
    3. 使用过期 `expected_effective_version=0` 生效 V1.1 会返回并发冲突；
    4. 使用正确 `expected_effective_version=1` 生效 V1.1 后，生效参数变为 `CHIP-B`；
    5. 版本级参数历史接口可返回 `V1.1` 对应记录；
    6. 联调产品数据已在验证后删除。

## 17. 十二次跟进：产品模块批次 C 已实施

- 跟进日期：2026-03-18
- 已实施范围：
  - 产品详情页改为基于版本列表显示“当前版本 / 当前版本状态 / 生效版本”，避免继续用 `effectiveVersion == currentVersion` 粗暴判断版本状态；
  - 产品详情页与参数管理历史弹窗补充版本号与操作类型展示，统一显示创建、复制、生效、停用、删除、回滚、编辑等动作；
  - 参数历史后端结果补齐版本标签，修正历史版本号展示偏移；
  - 产品列表导出改为 `/products/export/list`，并修正产品列表导出中的版本号格式；
  - 产品参数导出继续固定导出生效版本，并显式带版本列；
  - 删除保护从“仅阻止未完成工单”扩展到“任意生产工单、报废统计、维修订单、维修不良现象、维修原因记录”引用阻止删除；
  - 版本事件已写入参数历史表，使详情页与版本级历史可看到复制/生效/删除等记录。
- 关键文件：
  - `backend/app/services/product_service.py`
  - `backend/app/api/v1/endpoints/products.py`
  - `frontend/lib/pages/product_management_page.dart`
  - `frontend/lib/pages/product_parameter_management_page.dart`
  - `frontend/lib/services/product_service.dart`
- 验证结果：
  - 已执行 `python -m compileall backend/app`，通过；
  - 已执行 `flutter analyze`，通过；
  - 已执行 `flutter test test/models/product_models_test.dart test/services/product_service_test.dart test/widgets/product_module_issue_regression_test.dart`，全部通过；
  - 已通过真实接口联调验证：
    1. 产品参数总历史接口返回 `copy / activate / edit / create` 动作序列；
    2. 版本级历史接口返回正确的 `V1.1` 版本标签；
    3. 产品列表导出与生效参数导出都包含正确版本号；
    4. 直接向开发库写入一条已完成工单后，删除产品会被阻止；
    5. 清理测试工单后，产品删除恢复成功；
    6. 联调新增产品数据与工单数据已在验证后清理。

## 18. 十三次跟进：产品模块批次 D 已实施

- 跟进日期：2026-03-19
- 已实施范围：
  - 创建产品时不再直接把 `mes_product_parameter` 当作业务真源初始化，改为先建立版本参数真源，再同步当前参数投影；
  - 新建版本、复制版本、回滚版本已显式基于版本参数集合生成新版本快照，进一步降低对旧当前参数表的依赖；
  - `ensure_product_parameter_template_initialized()` 已降级为“从当前版本同步投影表”，不再承担模板真源维护职责；
  - 新增后端集成测试 `backend/tests/test_product_module_integration.py`，覆盖复制隔离、生效并发保护、历史/导出版本号、删除保护等主链路；
  - 批次 A-C 的前端回归测试继续通过。
- 关键文件：
  - `backend/app/services/product_service.py`
  - `backend/tests/test_product_module_integration.py`
  - `frontend/lib/services/product_service.dart`
  - `frontend/lib/pages/product_management_page.dart`
  - `frontend/lib/pages/product_parameter_management_page.dart`
  - `frontend/lib/pages/product_version_management_page.dart`
- 验证结果：
  - 已执行 `python -m compileall backend/app backend/tests`，通过；
  - 已执行 `python -m unittest discover -s backend/tests -p test_*.py`，3 个集成测试全部通过；
  - 已执行 `flutter analyze`，通过；
  - 已执行 `flutter test test/models/product_models_test.dart test/services/product_service_test.dart test/widgets/product_module_issue_regression_test.dart`，全部通过。
