# 后端测试执行与覆盖率报告

## 1. 执行日期
- 2026-03-06

## 2. 执行环境
- OS: Windows
- Python: 3.12.10
- 测试框架: pytest 9.0.2
- 覆盖率插件: pytest-cov 7.0.0

## 3. 执行命令
```powershell
.\.venv\Scripts\python.exe -m pytest test/backend --cov=backend/app --cov-report=term-missing --cov-report=xml
```

## 4. 执行结果
- 用例总数：55
- 通过：55
- 失败：0
- 全量覆盖率（`backend/app`）：**82%**
- 覆盖率文件：`coverage.xml`

## 5. 重点模块覆盖
- `app/services/user_service.py`: 90%
- `app/services/product_service.py`: 80%
- `app/services/equipment_service.py`: 74%
- `app/services/craft_service.py`: 68%
- `app/services/production_order_service.py`: 63%
- `app/services/production_execution_service.py`: 86%
- `app/services/quality_service.py`: 94%
- `app/services/production_statistics_service.py`: 95%

## 6. 主要结论
- 已满足“覆盖率至少 80%”目标。
- 核心业务流（用户、产品、工艺模板、生产执行、设备保养）均有集成测试覆盖。
- API endpoint 已补充直调用例，关键 CRUD 与异常映射已验证。

## 7. 后续可继续提升的方向
- 补齐 `production_order_service` 与 `craft_service` 的冲突分支、并发边界用例。
- 补齐 `equipment_service` 中更多过滤组合与异常状态迁移路径。
- 为 `startup_bootstrap` 增加更细粒度的 mock 测试（数据库创建与 alembic 调用链）。
