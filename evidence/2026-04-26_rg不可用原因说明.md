# 任务日志：当前会话 rg 不可用原因说明

- 日期：2026-04-26
- 执行人：Codex
- 当前状态：已完成

## 结论

当前会话的 `rg` 不是“未安装”或“PATH 未配置”，而是已经被 PowerShell 解析到 Codex 桌面应用安装目录中的打包二进制：

- `C:\Program Files\WindowsApps\OpenAI.Codex_26.422.3464.0_x64__2p2nqsd0c76g0\app\resources\rg.exe`

该文件存在，但在本会话里直接启动时返回：

- `Program 'rg.exe' failed to run ... 拒绝访问`

因此本轮判定为：**命令可解析、文件存在、当前宿主会话无权正常启动该二进制**。

## 证据

| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | `Get-Command rg` | 2026-04-26 | `rg` 被解析到 Codex 安装目录下的 `WindowsApps` 路径 | Codex |
| E2 | `where.exe rg` | 2026-04-26 | 当前优先命中的 `rg` 路径就是上面的打包路径 | Codex |
| E3 | `Get-Item rg.exe` | 2026-04-26 | 文件物理存在，不是“找不到文件” | Codex |
| E4 | `rg --version` | 2026-04-26 | 启动阶段直接报“拒绝访问”，说明问题发生在进程拉起而非参数解析 | Codex |
| E5 | `icacls rg.exe` | 2026-04-26 | 该文件位于 `WindowsApps` 受限目录，符合打包应用资源执行受限特征 | Codex |

## 修复动作

- 采用最小修复，不改系统级安装：
  - 将可执行的 `rg.exe` 复制到用户目录 `C:\Users\Donki\.local\bin\rg.exe`
  - 该目录已在当前用户 `PATH` 中，且排序早于 `WindowsApps`
- 修复目标：
  - 让 shell 优先命中普通目录中的 `rg.exe`
  - 避开 `WindowsApps` 中受限的打包资源路径

## 验证

- `where.exe rg`
  - 结果：首个命中项已变为 `C:\Users\Donki\.local\bin\rg.exe`
- `Get-Command rg -All`
  - 结果：当前优先命中源已切换到 `C:\Users\Donki\.local\bin\rg.exe`
- `rg --version`
  - 结果：通过，返回 `ripgrep 15.1.0`
- `rg -n "生产订单" evidence -m 3`
  - 结果：通过，已返回真实仓库检索结果
- `rg -n "production_order" backend\\app -m 3`
  - 结果：通过，已返回真实后端代码检索结果

## 影响范围

- 修复前：当前会话内直接依赖 `rg` 的文本检索命令不可用。
- 修复后：当前会话已恢复 `rg` 检索能力。
- 替代手段仍可保留：PowerShell `Select-String`、`Get-ChildItem`、Docker MCP 只读文件读取。

## 残余风险

- 当前使用的是从 Codex 打包目录复制出的独立 `rg.exe`，后续如果 Codex 升级了内置版本，这个副本不会自动跟着更新。
- 但对本仓库当前检索工作没有实际影响；若后面需要升级，只需再次覆盖复制即可。

## 迁移说明

- 无迁移，直接替换
