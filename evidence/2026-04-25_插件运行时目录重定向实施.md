# 任务日志：插件运行时目录重定向实施

- 日期：2026-04-25
- 执行人：Codex
- 当前状态：已完成
- 指挥模式：Subagent-Driven；主 agent 在隔离 worktree `C:\Users\Donki\Desktop\ZYKJ_MES\.worktrees\plugin-runtime-redirection` 上逐任务派发执行与双重复核

## 1. 输入来源

- 设计稿：`docs/superpowers/specs/2026-04-25-plugin-runtime-redirection-design.md`
- 实施计划：`docs/superpowers/plans/2026-04-25-plugin-runtime-redirection-implementation.md`
- 目标范围：
  - 统一插件根目录为 `plugins/`
  - 统一固定解释器为 `plugins/runtime/python312/python.exe`
  - 废弃 `frontend/build/.../runtime/python` 回退逻辑
  - 宿主在缺失插件目录或解释器时给出明确错误提示

## 2. 已完成项

1. `start_frontend.py` 已固定注入：
   - `MES_PLUGIN_ROOT=<repo>/plugins`
   - `MES_PYTHON_RUNTIME_DIR=<repo>/plugins/runtime/python312`
2. `PluginRuntimeLocator` 已收紧：
   - 默认插件根目录固定到仓库内 `plugins/`
   - 默认解释器固定到仓库内 `plugins/runtime/python312/python.exe`
   - 不再从 `frontend/build/.../runtime/python` 回退解释器
3. 已将官方 Python 3.12.10 Windows embeddable runtime 本体直接纳入：
   - `plugins/runtime/python312/`
4. `.gitignore` 已收口：
   - 运行时本体和插件 `vendor/` 可提交
   - runtime/vendor 下的日志、临时文件和系统噪音继续忽略
5. `README.md` 与 `plugins/runtime/python312/README.md` 已补维护口径。
6. 宿主在启动前已显式检查：
   - 插件根目录是否存在
   - Python 运行时是否存在
7. 宿主在错误态会直接显示明确文案：
   - `插件目录缺失`
   - `Python 运行时缺失`
8. `openPlugin()` 已处理迟到启动结果的回收，避免取消/切换后留下孤儿进程。
9. 由于 Python embeddable runtime 处于隔离模式，宿主不再把 `PYTHONPATH` 作为主依赖装配机制，而是传递：
   - `MES_PLUGIN_DIR`
   - `MES_PLUGIN_VENDOR_DIR`
   - `MES_PLUGIN_APP_DIR`
   并由插件 `launcher.py` 自行组装 `sys.path`
10. `serial_assistant/manifest.json` 已收紧为 Python 3.12 口径。

## 3. 执行与复核记录

### Task 1：固定启动环境与运行时定位规则

- 实现提交：`3ca6d66`
- 规格复核：通过
- 代码质量复核：经一轮返修后通过

### Task 2：运行时目录入仓与忽略规则收口

- 实现提交：`0040fc8`
- 返修提交：`64a624e`
- 规格复核：通过
- 代码质量复核：经一轮返修后通过

### Task 3：宿主缺失目录/解释器错误态

- 中间提交：`a6e2478`
- 最终实现包含于当前工作树待收口改动
- 规格复核：通过
- 代码质量复核：经一轮返修后通过

## 4. 交付判断

- 已完成项：
  - 固定运行时目录与解释器路径
  - 运行时本体入仓
  - 宿主错误态收口
  - 自动化验证
  - 运行级启动验证
- 未完成项：无
- 最终结论：可交付

## 5. 迁移说明

- 无迁移，直接替换
