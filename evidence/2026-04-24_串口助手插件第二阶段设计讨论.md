# 任务日志：串口助手插件第二阶段设计讨论

- 日期：2026-04-24
- 执行人：Codex
- 当前状态：进行中
- 指挥模式：本轮为设计讨论；当前会话未启用子 agent，采用“调研 -> 设计 -> 用户确认 -> 留痕”分阶段补偿

## 1. 输入来源

- 用户指令：
  - 继续完善这个插件吧
  - 已拒绝可视化伴随功能，采用文本方式讨论
- 需求基线：
  - `AGENTS.md`
  - `docs/AGENTS/00-导航与装配说明.md`
  - `docs/AGENTS/10-执行总则.md`
  - `docs/AGENTS/20-指挥官模式与工作流.md`
  - `docs/AGENTS/30-工具治理与验证门禁.md`
  - `docs/AGENTS/40-质量交付与留痕.md`
  - `docs/AGENTS/50-模板与索引.md`
- 排查范围：
  - `frontend/lib/features/plugin_host/`
  - `plugins/serial_assistant/`
  - `frontend/test/widgets/plugin_host_page_test.dart`
  - `frontend/test/services/plugin_runtime_locator_test.dart`

## 1.1 前置说明

- 默认主线工具：`brainstorming`、`update_plan`、宿主安全命令（PowerShell / git）
- 缺失工具：独立 `Sequential Thinking`、结构化检索 MCP、浏览器验证入口、可用 `rg.exe`
- 缺失/降级原因：
  - 当前会话未提供独立 `Sequential Thinking`、结构化检索 MCP 与浏览器验证入口
  - `rg.exe` 在当前宿主路径启动时被拒绝访问
- 替代工具：
  - 使用 `update_plan` 做等效拆解
  - 使用 PowerShell 文本检索与现有测试文件补偿

## 2. 当前判断

1. 当前插件中心已经能发现 `串口助手`，说明宿主扫描、入口与基础承载链路已经打通。
2. 当前右侧工作区仍处在“插件详情/占位”阶段，尚未切到更完整的插件工作台体验。
3. 当前串口助手插件本身已有最小 UI 与本地服务，但还没有进入“可长期使用的正式工具”阶段。
4. 用户已明确第二阶段优先级：`先打通宿主承载`，即优先让右侧工作区真正打开插件页面，而不是继续停留在占位说明。
5. 用户已确认第二阶段交互采用：`点击左侧插件项后，右侧直接打开插件页面`。
6. 用户已确认第二阶段生命周期采用：`单插件单实例`，并接受“关闭=结束进程、重启=先停后起”的最小宿主管理策略。
7. 用户已确认右侧在插件真正就绪前使用统一的宿主启动面板/异常面板，不再显示简介占位卡。
8. 用户已确认串口助手第一屏采用“三段工作台”布局：连接区、发送区、接收日志区，且第一页不塞高级参数。
9. 用户已确认第二阶段范围收敛为：先改宿主承载状态流，再整理串口助手第一页，不扩展高级参数与平台能力。
10. 用户已确认第二阶段实施策略：目标采用“方案 B，做出完整闭环”，执行节奏采用“方案 A 的顺序，先宿主，后插件”。

## 3. 当前状态

- 已完成：
  - 读取当前插件中心页面与串口助手前端代码
  - 确认可视化伴随功能已被用户拒绝
  - 已形成正式 spec：`docs/superpowers/specs/2026-04-24-serial-assistant-phase2-design.md`
  - 已完成 spec 自检，未发现占位词、内部矛盾与范围失控
- 进行中：
  - 等待用户审阅正式 spec

## 4. 迁移说明

- 无迁移，直接替换
