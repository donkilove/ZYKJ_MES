# 规则与工具环境整改清单

- 日期：2026-04-09
- 依据：`AGENTS.md`、`.aiassistant/rules/AGENTS.md`、`docs/opencode_tooling_bundle.md`、`docs/host_tooling_bundle.md`、`evidence/task_log_20260409_rules_tool_deep_check.md`
- 目标：把“规则要求”“文档声明”“当前会话实际能力”三者重新对齐。

## 1. 该改的文档

| 优先级 | 文件 | 问题归类 | 必改内容 | 完成标准 |
| --- | --- | --- | --- | --- |
| 高 | `AGENTS.md` | 规则过硬 + 文档失真 | 把 `Sequential Thinking`、`Serena`、`Context7`、`web.run`、`update_plan` 的强制或默认主链表述改成“默认优先，当前会话不可用时按替代链降级并留痕”。 | 根规则不再要求必须存在当前会话没有的同名工具。 |
| 高 | `.aiassistant/rules/AGENTS.md` | 规则源冲突 | 删除、改为镜像说明，或在首屏明确“从属副本，以根目录 `AGENTS.md` 为准”，并与根规则同步。 | 仓库只保留一个权威规则源，或明确主从关系且内容一致。 |
| 高 | `docs/opencode_tooling_bundle.md` | 文档失真 | 把“已连接 / 已接入 / 默认启用”改成“设计上支持 / 需宿主注入 / 当前状态以实测为准”；补充当前会话是否暴露该能力。 | 文档状态字段能区分“仓库侧脚本存在”“宿主已配置”“当前会话已暴露”。 |
| 中 | `docs/host_tooling_bundle.md` | 文档边界不清 | 补一句“宿主已安装或已验证，不等于当前 agent 会话已注入可调用”。 | 文档不再把宿主安装状态误导成当前会话可直接使用。 |
| 中 | 其他命中 `已连接`、`已接入`、`默认启用`、`唯一权威规则源` 的说明文档 | 同步项遗漏风险 | 全仓检索后同步改写相关表述，避免旧描述残留。 | 全仓相关表述完成一次同步收敛。 |

## 2. 该补的 MCP / 接入

| 优先级 | 能力 | 当前缺口 | 补齐动作 | 验收标准 |
| --- | --- | --- | --- | --- |
| 高 | 宿主 MCP 注册机制 | `opencode-cli.exe mcp list` 当前为 `No MCP servers configured` | 先补统一注册或启动配置，再补逐项能力。 | `opencode-cli.exe mcp list` 不为空，且条目与文档一致。 |
| 高 | `sequential_thinking` | 规则要求编码前优先使用，但当前会话无同名工具。 | 注入宿主 MCP，或明确改为人工拆解默认替代。 | 能完成一次最小思考调用，或规则正式降级。 |
| 高 | `serena` | 规则把它作为代码定位与精确编辑主链，但当前会话无同名工具。 | 注入宿主 MCP，或正式改为本地搜索编辑链替代。 | 能完成一次最小符号定位或规则正式降级。 |
| 高 | `postgres` | 文档称“已接入，默认启用”，但当前实测无 MCP 列表项。 | 修正宿主注入与连接配置。 | MCP 列表可见 `postgres`，且至少一次只读探测成功。 |
| 中 | `context7` | 文档称已连接，但当前会话无同名工具。 | 注入宿主 MCP，或明确以仓库文档 + `webfetch` 降级。 | 能完成一次最小文档抓取或规则正式降级。 |
| 中 | `playwright` | 文档称已连接，但当前会话无同名工具。 | 注入宿主 MCP，或从默认链路降为按需补充。 | MCP 列表可见 `playwright`，且能完成一次最小页面打开。 |

## 3. 该降级或改写的规则表述

| 优先级 | 位置 | 现状问题 | 建议改写方向 |
| --- | --- | --- | --- |
| 高 | `AGENTS.md` / `.aiassistant/rules/AGENTS.md` 4.1 `编码前必须完成 Sequential-Thinking 分析` | 当前会话无该同名工具，按字面不可执行。 | 改为“默认优先使用；不可用时必须在 `evidence/` 记录书面拆解、降级原因、替代动作与残余风险”。 |
| 中 | `AGENTS.md` / `.aiassistant/rules/AGENTS.md` 4.1 `update_plan` 或 `TodoWrite` | 当前会话只有 `TodoWrite`。 | 改为“优先使用当前会话可见的计划工具；若均不可用，则写入 `evidence/`”。 |
| 高 | `AGENTS.md` / `.aiassistant/rules/AGENTS.md` 7.1 默认工具口径 | `Serena`、`Context7`、`web.run` 与当前环境不一致。 | 改为“首选宿主直连工具；不可用时退回 `glob`、`grep`、`read`、`bash`、`webfetch`、`pycharm_*`、项目包装命令”。 |
| 高 | `AGENTS.md` / `.aiassistant/rules/AGENTS.md` 7.2 分类与默认验证 | 多个分类把固定工具写死为主链。 | 每类改成“默认优先工具 + 降级替代工具 + 最低证据要求”。 |
| 中 | `AGENTS.md` / `.aiassistant/rules/AGENTS.md` 7.4 工具降级规则 | 只覆盖少数工具不可用，未覆盖“宿主未注入 / 当前会话未暴露”。 | 增补“未注入、未暴露、权限缺失、返回 schema mismatch”等场景的替代链。 |
| 高 | `AGENTS.md` / `.aiassistant/rules/AGENTS.md` 唯一规则源表述 | 仓库内实际存在两份 `AGENTS.md`。 | 明确主从关系，或移除副本，恢复“唯一规则源”成立条件。 |
| 中 | `AGENTS.md` / `.aiassistant/rules/AGENTS.md` 中 `web.run` 相关表述 | 当前会话实际可用的是 `webfetch`。 | 改成泛化口径：“外部网页抓取工具，如 `webfetch` 或等效能力”。 |

## 4. 推荐执行顺序

1. 先处理规则源冲突：统一根目录 `AGENTS.md` 与 `.aiassistant/rules/AGENTS.md` 的关系。
2. 修 `docs/opencode_tooling_bundle.md`：去掉失真的“已连接 / 已接入 / 默认启用”强断言。
3. 补宿主 MCP 注册：至少让 `sequential_thinking`、`serena`、`postgres` 先可见。
4. 回改规则表述：把硬依赖改成“默认优先 + 降级替代 + evidence 留痕”。
5. 修 `docs/host_tooling_bundle.md` 与其他同步文档，统一“宿主安装”和“当前会话可调用”的边界。

## 5. 完成判定

1. `opencode-cli.exe mcp list` 不再为空，且与工具文档声明一致。
2. `docs/opencode_tooling_bundle.md` 的状态字段能明确区分：仓库脚本存在、宿主已配置、当前会话已暴露。
3. 根规则不再把当前会话缺失的工具写成无法替代的硬门禁。
4. 仓库内不再同时存在两个“唯一权威规则源”。
5. 至少完成一次全仓检索，收敛相关失真表述。

## 6. 最小验收命令口径

```powershell
& "C:\Users\Donki\AppData\Local\OpenCode\opencode-cli.exe" mcp list
python "tools/project_toolkit.py" --help
```

## 7. 迁移说明

- 无迁移，直接替换。
