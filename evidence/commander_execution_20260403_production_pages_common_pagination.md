# 指挥官执行留痕：生产模块列表页统一公共组件与公共翻页（2026-04-03）

## 1. 任务信息

- 任务名称：生产模块列表页统一公共组件与公共翻页
- 执行日期：2026-04-03
- 执行方式：指挥官模式拆解调度 + 执行子 agent + 独立验证子 agent
- 当前状态：进行中

## 2. 输入来源

- 用户要求：以下页面与“生产订单管理”一致，列表使用公共组件，同时接入公共翻页组件。
- 本轮目标页（基于用户截图）：
  1. `生产订单查询`
  2. `代班记录`
  3. `报废统计`
  4. `维修订单`
  5. `并行实例追踪`

## 3. 初步原则

1. 优先复用：`CrudPageHeader`、`CrudListTableSection`、`UnifiedListTableHeaderStyle`、`SimplePaginationBar`。
2. 不擅自改变业务过滤条件、按钮权限、导出行为与详情跳转。
3. 若后端已支持分页，则接真实分页；若某页后端不支持分页，需要在执行前明确是否补后端契约。
4. 所有分页行为遵循统一规则：
   - 翻页保留当前筛选
   - 搜索/筛选变化回第一页
   - 刷新保留当前页
   - 当前页越界自动回退

## 4. 指挥官编排

1. 先盘点目标页当前公共组件与分页支持状态。
2. 再按页面复杂度拆成两批执行与验证。

## 5. 执行记录

- 2026-04-03：任务启动，准备派发盘点子任务。
- 2026-04-03：盘点结论已确认。
  1. 目标页均已使用 `CrudListTableSection` 与统一表头样式。
  2. 目标页均未接入 `SimplePaginationBar`，当前前端仍是固定第一页模式。
  3. 对应后端接口均已支持真实分页，缺口仅在前端页码状态与公共翻页组件接线。
- 2026-04-03：实施拆分。
  1. 任务 PAG1：先改 `代班记录`、`报废统计`、`维修订单`。
  2. 任务 PAG2：再改 `生产订单查询`、`并行实例追踪`。
- 2026-04-03：PAG1 执行完成。`代班记录`、`报废统计`、`维修订单` 已接入真实分页状态与公共翻页组件 `SimplePaginationBar`，并保持 `CrudListTableSection`；目标 widget tests 与 `flutter analyze` 通过，待独立验证。
- 2026-04-03：PAG1 独立验证结论：`PASS`。3 个标准列表页已保持 `CrudListTableSection` 并接入 `SimplePaginationBar`，当前页状态、搜索/筛选回第一页、刷新保留当前页、越界页自动回退均已到位，允许进入 PAG2。
- 2026-04-03：PAG2 执行完成。`生产订单查询` 与 `并行实例追踪` 已接入真实分页状态和公共翻页组件 `SimplePaginationBar`，并保持 `CrudListTableSection` 与原有业务入口；目标 widget tests 与 `flutter analyze` 通过，待独立验证。
- 2026-04-03：PAG2 独立验证结论：`PASS`。`生产订单查询` 与 `并行实例追踪` 均已保持 `CrudListTableSection` 并接入 `SimplePaginationBar`，搜索/筛选回第一页、刷新保留当前页、自动刷新保留当前页（订单查询）与越界页自动回退均已生效。

## 6. 最终结论

- 本轮目标页均已统一满足：
  1. 列表继续使用公共列表组件 `CrudListTableSection`
  2. 列表接入公共翻页组件 `SimplePaginationBar`
  3. 前端不再固定第一页，改为真实分页
- 覆盖页面：
  1. `生产订单查询`
  2. `代班记录`
  3. `报废统计`
  4. `维修订单`
  5. `并行实例追踪`
