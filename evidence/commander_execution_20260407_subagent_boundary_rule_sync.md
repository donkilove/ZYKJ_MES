# 指挥官任务日志

## 1. 任务信息

- 任务名称：补写子 agent 生命周期与越位回执处理规则
- 执行日期：2026-04-07
- 执行方式：规则文件核对 + 最小文档补丁 + 主 agent 留痕
- 当前状态：已完成
- 指挥模式：主 agent 统筹，子 agent 只读定位，主 agent 汇总与落盘

## 2. 输入来源

- 用户指令：
  1. 将“子 agent 用完即关”写进规则。
  2. 将“若回执出现越位口吻，直接丢弃或重派，不原样暴露给用户”写进规则。
  3. 完成后继续刚才被中断的工作。

## 3. Sequential Thinking 留痕

- 执行时间：2026-04-07
- 结论摘要：
  1. 应优先更新最高优先级规则文件 `AGENTS.md`。
  2. 需同时核对聚合手册与指挥官流程文档，避免规则漂移。
  3. 若聚合文档已包含该规则，则只补齐缺失处，保持最小改动。

## 4. 调研与降级记录

- 使用 `Get-ChildItem` 与 `Get-Content` 定位并读取规则文档。
- 原计划使用 `rg` 检索相关条目，但本地 `rg.exe` 启动遭遇“拒绝访问”，因此降级为 PowerShell 原生命令检索。
- 降级影响：
  - 仅影响检索效率，不影响结论正确性。
- 补偿措施：
  - 改用精确文件读取与 `Select-String` 定位关键章节。

## 5. 文件核对结果

- `AGENTS.md`
  - 先前缺少这两条顶层强制规则。
  - 本次已补齐。
- `docs/commander/项目规则与指挥官模式统一手册.md`
  - 已存在对应规则，无需重复修改。
- `docs/commander/指挥官工作流程.md`
  - 已在“6.3 输出契约”中存在更细的执行边界，无需重复修改。

## 6. 实际改动

- 修改文件：`AGENTS.md`
- 新增规则 1：
  - 指挥官模式下，子 agent 在当前原子任务完成、回执被主 agent 接收或被判定废弃后，必须立即关闭，不得保留空闲实例常驻。
- 新增规则 2：
  - 若子 agent 回执出现越位口吻、角色冒用、主 agent 对外话术或不符合内部摘要契约，主 agent 必须直接丢弃该回执或重新派发，不得原样暴露给用户。

## 7. 证据表

| 证据编号 | 来源 | 适用结论 |
| --- | --- | --- |
| E-001 | `AGENTS.md` | 顶层强制规则已补齐子 agent 生命周期与越位回执处理要求 |
| E-002 | `docs/commander/项目规则与指挥官模式统一手册.md` | 聚合手册中对应规则已存在，无需重复补丁 |
| E-003 | `docs/commander/指挥官工作流程.md` | 细化流程文档中已存在子 agent 输出契约与立即关闭约束 |

## 8. 最终结论

- 本次已将用户要求的两条规则写入最高优先级文件 `AGENTS.md`。
- 聚合手册与指挥官流程文档已具备对应约束，当前文档口径一致。
- 后续继续执行指挥官模式时，应严格执行：
  1. 子 agent 用完即关。
  2. 越位回执直接丢弃或重派，不原样暴露给用户。
