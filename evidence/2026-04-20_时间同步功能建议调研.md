# 任务日志：时间同步功能建议调研

- 日期：2026-04-20
- 执行人：Codex
- 当前状态：进行中

## 1. 输入来源
- 用户指令：进入下一个功能开发，评估“时间同步功能，前端默认启用”的可行性与建议

## 1.1 前置说明
- 默认主线工具：`using-superpowers`、`brainstorming`、`update_plan`、宿主安全命令、项目文件检索
- 缺失工具：MCP 资源、`Sequential Thinking` 直接入口
- 缺失/降级原因：
  - 当前会话 `list_mcp_resources` / `list_mcp_resource_templates` 返回空结果
  - 当前工具列表未暴露 `Sequential Thinking` 专用工具
- 替代工具：书面拆解、`update_plan`、本地代码核对、`evidence`
- 影响范围：本轮先做文本化方案判断与设计讨论，不进入实现

## 2. 当前已核对上下文
- `frontend/lib/main.dart`
  - 当前软件设置已提升到应用层，登录页、改密页、主壳层共享同一份 `SoftwareSettingsController`
- `frontend/lib/features/settings/models/software_settings_models.dart`
  - 当前已有主题、密度、启动页、侧边栏偏好四类软件设置枚举/模型
- `frontend/lib/features/settings/presentation/software_settings_controller.dart`
  - 当前软件设置支持启动加载、立即持久化与错误提示
- `frontend/lib/features/settings/presentation/software_settings_page.dart`
  - 当前软件设置页只有“外观”“布局偏好”两个分区，具备继续扩展分区的结构基础
- `backend/app/main.py`
  - 当前仅有 `/health` 健康检查接口，尚无专门返回“服务器当前时间”的接口

## 3. 初步判断
- “时间同步功能”值得做，但不建议做成“前端主动改系统时间”。
- 更适合做成：
  1. 默认启用的“服务器时间校准/偏移补偿”
  2. 配套的时间异常提示与状态展示
  3. 必要时附带“手动立即同步”入口

## 4. 新增技术结论
- 如果要以后端服务器时间作为权威时间源，并且最终推动 Windows 系统时间修正，则至少要新增一条后端时间接口，例如：
  - 返回服务器当前 UTC 时间
  - 返回服务器时区/偏移
  - 最好附带采样时间戳，便于前端估算网络往返延迟

## 5. 当前待澄清的关键问题
- 你说的“时间同步”，目标到底是：
  1. 只让软件内显示和业务提交都按服务器时间对齐
  2. 还是希望前端直接推动操作系统时间被修正

## 6. 用户最新确认
- `2026-04-20 10:15:21 +08:00`
- 用户选择：`2. 希望软件还能直接把电脑系统时间改正确`
- 当前判断：
  - 这会把功能边界从“软件内时间校准”扩大到“桌面端系统能力调用”
  - 后续设计必须明确：
    - 权威时间源是谁
    - 是否需要管理员权限
    - 无权限或对时失败时如何降级
    - 是否保留软件内偏移校准作为兜底

- `2026-04-20 10:17:42 +08:00`
- 用户选择：`启动后自动检查，偏差超过阈值就自动尝试修正`
- 当前判断：
  - 这会把功能体验定义为“默认开启的主动修正”
  - 后续设计必须明确：
    - 偏差阈值是多少
    - 无权限、失败、被系统拦截时的降级动作
    - 是否允许静默失败，还是必须提示用户

- `2026-04-20 10:22:26 +08:00`
- 用户选择：`无权限或失败时，弹出明确告警，并退化为仅软件内时间校准`
- 当前判断：
  - 这会让功能形成双层兜底：
    1. 优先尝试修正 Windows 系统时间
    2. 失败后继续保证软件内业务时间与服务器对齐
  - 后续设计必须明确：
    - Windows 改时失败后的告警位置与文案
    - 退化后哪些前端显示/提交逻辑切换到“服务器偏移时间”
    - 是否要求下次启动继续自动重试系统改时

- `2026-04-20 10:24:41 +08:00`
- 用户选择：`接受管理员权限 / UAC 提权`
- 当前判断：
  - 这意味着功能可以合法尝试修改 Windows 系统时间，不必被限制在纯前端显示层
  - 后续设计需要明确：
    - 提权触发时机是否只在偏差超阈值时发生
    - UAC 被取消时是否立即退化为软件内时间校准
    - 是否需要单独辅助进程来承接提权后的系统时间修改

- `2026-04-20 10:26:12 +08:00`
- 用户选择：`偏差超过 30 秒才触发自动修正`
- 当前判断：
  - 该阈值能显著降低启动时的误触发与 UAC 打扰频率
  - 下一步进入实现路线对比：
    - 主程序自提权直改系统时间
    - 主程序调用本地提权助手
    - 独立 Windows 服务常驻改时

- `2026-04-20 10:35:17 +08:00`
- 用户选择：`主程序直接提权改时间`
- 当前判断：
  - 首版可以不引入额外本地助手或常驻服务，先把“检测 -> 提权 -> 改时 -> 回执 -> 降级”主链路打通
  - 接下来进入正式设计分段呈现：
    - 整体架构与启动流程
    - 软件设置与用户提示
    - 后端时间接口与前端校准逻辑
    - 失败降级与测试策略

- `2026-04-20 10:36:36 +08:00`
- 用户反馈：已确认“整体架构与启动主链路”设计段落
- 当前判断：
  - 可以继续下一段“软件设置与用户提示”设计

- `2026-04-20 10:39:24 +08:00`
- 用户反馈：已确认“软件设置与用户提示”设计段落
- 当前判断：
  - 可以继续下一段“后端时间接口与前端校准逻辑”设计

- `2026-04-20 10:42:12 +08:00`
- 用户反馈：已确认“后端时间接口与前端校准逻辑”设计段落
- 当前判断：
  - 可以继续最后一段“失败降级、UAC 回执与测试策略”设计

- `2026-04-20 10:42 之后 +08:00`
- 用户反馈：已确认“失败降级、UAC 回执与测试策略”设计段落
- 当前判断：
  - 四段设计已全部确认，可以收敛为正式 spec

## 7. 产出与自检
- 已写入规格文件：
  - `docs/superpowers/specs/2026-04-20-time-sync-design.md`
- 自检动作：
  - 对规格文件执行占位词扫描：`TODO|TBD|待定|待补|稍后|后续再说|可自行|适当|大概|也许`
- 自检结果：
  - 未命中占位词
  - 规格已明确：
    - 权威时间源
    - 自动修正阈值
    - UAC 提权策略
    - 失败降级路径
    - 前后端边界
    - 测试策略
- 下一步：
  - 提交 spec
  - 请求用户审阅 spec

## 8. 实施计划与执行切换
- `2026-04-20 11:11:45 +08:00`
- 已写入实施计划：
  - `docs/superpowers/plans/2026-04-20-time-sync-implementation.md`
- 实施计划自检动作：
  - 对照 `docs/superpowers/specs/2026-04-20-time-sync-design.md` 做规格覆盖检查
  - 对实施计划执行占位词扫描：`TODO|TBD|待定|待补|稍后|后续再说|可自行|适当|大概|也许|similar to|Same as|Write tests for the above|appropriate error handling|implement later`
  - 对关键命名执行一致性检查：`TimeSyncController`、`WindowsTimeSyncService`、`EffectiveClock`、`MessageCenterPage.nowProvider`、`defaultApiBaseUrl`
- 实施计划自检结果：
  - 未命中占位词
  - 已修正一处命名不一致：
    - 将架构摘要中的 `WindowsSystemTimeSyncService` 统一为 `WindowsTimeSyncService`
  - 已补齐两处规格覆盖缺口：
    - 在启动链路任务中补入失败告警展示与测试，覆盖“系统改时失败/服务器时间不可用时明确告警”的需求
    - 在设置页任务中补入状态卡片字段：服务器时间、本机时间、最近同步结果、最近同步时间
  - 已补齐一处关键业务路径落点：
    - 在消息中心任务中补入 `nowProvider` / `EffectiveClock` 接入，确保“软件内时间校准”先落到真实业务页面，而不是只停留在状态模型
  - 已修正两处计划可执行性问题：
    - 为 `app_bootstrap_page_test.dart` 片段补齐 `runtime_endpoints` 与时间同步模型 import
    - 为 `software_settings_page_test.dart` 片段补齐预置快照与 `checkAtStartup()` 调用，避免状态卡片断言落空
- 执行方式收口：
  - 标准技能模板原本同时支持 `Subagent-Driven` 与 `Inline Execution`
  - 用户已明确要求：后续任务不再派发子 AGENT，只能在当前对话内直接执行
  - 本轮后续统一采用：
    - 书面拆解 + `update_plan`
    - 当前会话内按实施计划直接执行
    - 修复阶段与验证阶段显式分离作为无子 agent 的补偿措施
- 下一步：
  - 提交实施计划与本次 evidence 更新
  - 立即进入按计划实施

## 9. 实施进度
- `2026-04-20 11:20:00 +08:00`
- 已完成原子任务：
  - 任务 1：新增后端服务器时间接口并同步契约
- 实际改动：
  - 新增 `backend/app/api/v1/endpoints/system.py`
  - 新增 `backend/app/schemas/system.py`
  - 修改 `backend/app/api/v1/api.py`
  - 更新 `backend/openapi.generated.json`
  - 新增 `backend/tests/test_system_time_endpoint_unit.py`
- TDD 验证记录：
  - 红灯：
    - 运行 `python -m pytest tests/test_system_time_endpoint_unit.py -q`
    - 结果：`404 != 200`
    - 结论：确认失败原因是 `/api/v1/system/time` 尚未注册
  - 绿灯：
    - 再次运行 `python -m pytest tests/test_system_time_endpoint_unit.py -q`
    - 结果：`1 passed`
    - 结论：接口已可匿名访问，返回结构符合预期
  - 契约同步：
    - 运行 `python -c "import json; from app.main import app; print(json.dumps(app.openapi(), ensure_ascii=False, indent=2))" > openapi.generated.json`
    - 使用 `rg` 核对 `/api/v1/system/time`
    - 结果：`openapi.generated.json` 已包含新路径
- 工具降级补充：
  - `pytest` 可执行别名在当前 PowerShell 会话中不可用
  - 替代命令：`python -m pytest`
  - 影响范围：仅影响命令入口写法，不影响测试执行与结论
- git 提交：
  - `ee812ab 新增服务器时间接口`
- 当前下一步：
  - 继续任务 2：扩展软件设置持久化与默认接口地址

## 10. 迁移说明
- 无迁移，直接替换
