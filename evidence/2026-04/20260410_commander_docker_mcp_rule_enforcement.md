# 任务日志：Docker MCP 默认强制规则整改

- 日期：2026-04-10
- 执行人：OpenCode 主 agent
- 当前状态：已完成
- 指挥模式：主 agent 拆解调度，子 agent 执行，独立子 agent 验证

## 1. 输入来源
- 用户指令：修改项目规则，强制使用 Docker 提供的 MCP 工具；若没有，需要在前置说明中写出；因涉及规则，要求仔细检查风险。
- 需求基线：`AGENTS.md`、`docs/opencode_tooling_bundle.md`
- 代码范围：项目规则与工具接入说明文档

## 2. 任务目标、范围与非目标
### 任务目标
1. 将项目规则调整为默认强制优先使用 Docker 提供的 MCP 工具。
2. 若所需 Docker MCP 缺失、不可用或受更高优先级约束，要求在前置说明中明确写出，并在留痕中记录。
3. 清理默认工具口径中对 `Serena`、本地 `postgres-mcp` 的主线依赖，避免规则冲突。

### 任务范围
1. 调整 `AGENTS.md` 中的通用执行约束、工具治理、分类口径、降级规则与模板。
2. 同步调整 `docs/opencode_tooling_bundle.md` 中的默认工具来源描述。
3. 执行独立验证，确认不存在明显冲突和旧主线残留。

### 非目标
1. 不改 Docker Desktop 中 MCP 服务器的安装状态。
2. 不改系统级、开发者级工具约束。
3. 不补做与本次规则无关的文档重写。

## 3. 证据编号表
| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | `AGENTS.md` 现状核对 | 2026-04-10 | 现有默认工具口径与分类表大量以 `Serena` 为主线，和 Docker MCP 默认强制目标冲突 | OpenCode |
| E2 | `docs/opencode_tooling_bundle.md` 现状核对 | 2026-04-10 | 文档仍保留本地 `serena` 与 `postgres-mcp` 的默认接入口径，需要同步整改 | OpenCode |
| E3 | 风险评估结论 | 2026-04-10 | 规则不可写成“无条件只能用 Docker MCP”，必须保留更高优先级约束与降级补偿口径 | OpenCode |
| E4 | 执行子 agent 改动回执 | 2026-04-10 | `AGENTS.md` 与 `docs/opencode_tooling_bundle.md` 已完成 `MCP_DOCKER` 主线整改 | OpenCode |
| E5 | 验证子 agent 独立复核 | 2026-04-10 | 规则整改通过，无阻塞问题 | OpenCode |
| E6 | `opencode mcp list` 与主文档残留检索 | 2026-04-10 | 当前宿主仅显示 `MCP_DOCKER`；目标文档中无 `Serena` 默认主线残留 | OpenCode |
| E7 | 工具化验证日志 | 2026-04-10 | 任务完成闭环，门禁通过 | OpenCode |

## 4. 指挥拆解结果
| 序号 | 原子任务 | 目标 | 执行子 agent | 验证子 agent | 验收标准 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 规则主线整改 | 将 `AGENTS.md` 改为默认强制优先 `MCP_DOCKER`，并补齐前置说明与降级留痕要求 | 已完成 | 已完成 | 关键章节完成替换，且不与更高优先级约束冲突 | 已完成 |
| 2 | 支撑文档同步 | 将 `docs/opencode_tooling_bundle.md` 调整为 Docker MCP 默认主线，本地包装命令降为补偿口径 | 已完成 | 已完成 | 文档默认来源与规则一致 | 已完成 |
| 3 | 独立复核 | 检查旧口径残留、前置说明要求、模板闭环与风险表述 | 已完成 | 已完成 | 有独立通过/不通过结论与证据 | 已完成 |

## 5. 风险预判
- 风险 1：若直接写成“只能使用 Docker MCP”，会与系统级、开发者级更高优先级工具约束冲突。
- 风险 2：若只改总则不改分类表和降级规则，会形成双重口径，执行不可判定。
- 风险 3：若只改 `AGENTS.md` 不改 `docs/opencode_tooling_bundle.md`，会导致支撑文档继续误导执行。
- 风险 4：若只要求前置说明，不要求 `evidence` 留痕，会破坏现有审计链。

## 6. 子 agent 输出摘要
- 调研摘要：主 agent 已完成基线核对与风险预判，待执行子 agent 落地改动。
- 执行摘要：执行子 agent 已完成两份文档的主线整改。`AGENTS.md` 已将通用执行约束、默认工具口径、分类表、降级规则与模板统一收敛为 `MCP_DOCKER` 主线；`docs/opencode_tooling_bundle.md` 已改为默认优先 `MCP_DOCKER`，并将本地包装命令降级为补偿工具。
- 验证摘要：验证子 agent 只读复核 `AGENTS.md` 的 `4.1`、`7.1`、`7.2`、`7.4`、`10.1`、`10.2` 以及 `docs/opencode_tooling_bundle.md` 全文，结论为“通过”；补充运行 `opencode mcp list` 确认当前宿主仅显示 `MCP_DOCKER`，并用文本检索确认目标文档中无 `Serena` 默认主线残留。

## 6.1 失败重试记录
| 轮次 | 原子任务 | 失败现象 | 根因判断 | 修复动作 | 复检结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 独立复核 | 首个验证子 agent 回执为空 | 子 agent 未返回有效结果 | 重派新的验证子 agent 执行只读复核 | 通过 |

## 7. 工具降级、硬阻塞与限制
- 不可用工具：无
- 降级原因：无
- 替代流程：无
- 影响范围：无
- 补偿措施：无
- 硬阻塞：无

## 8. 交付判断
- 已完成项：
  - 调整 `AGENTS.md`，将默认工具口径、分类表、降级规则、模板统一切换为 `MCP_DOCKER` 主线
  - 调整 `docs/opencode_tooling_bundle.md`，将 Docker 宿主 MCP 改为默认来源，本地包装命令改为降级/补偿口径
  - 独立验证通过，并补充运行时 `MCP_DOCKER` 证据
- 未完成项：无
- 是否满足任务目标：是
- 主 agent 最终结论：可交付

## 9. 迁移说明
- 无迁移，直接替换
