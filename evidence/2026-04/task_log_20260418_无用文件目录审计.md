# 任务日志：项目无用文件与目录审计

- 日期：2026-04-18
- 执行人：Codex
- 当前状态：已完成
- 指挥模式：单会话审计闭环；受上层指令约束未派发子 agent，采用“分析与复核显式分离”的降级补偿

## 1. 输入来源

- 用户指令：列出项目中无用的文件、文件夹，重点关注类似 `desktop_tests` 的候选。
- 需求基线：
  - `AGENTS.md`
  - `docs/AGENTS/00-导航与装配说明.md`
  - `docs/AGENTS/10-执行总则.md`
  - `docs/AGENTS/20-指挥官模式与工作流.md`
  - `docs/AGENTS/30-工具治理与验证门禁.md`
  - `docs/AGENTS/40-质量交付与留痕.md`
  - `docs/AGENTS/50-模板与索引.md`
- 代码范围：
  - 仓库根目录
  - `desktop_tests/`
  - `backend/`
  - `frontend/`
  - `tools/`
  - `docs/`

## 1.1 前置说明

- 默认主线工具：`Sequential Thinking`、`update_plan`、PowerShell、安全文本检索（`rg`/`git ls-files`）
- 缺失工具：无
- 缺失/降级原因：无
- 替代工具：无
- 影响范围：无

## 2. 任务目标、范围与非目标

### 任务目标

1. 找出项目内疑似无用、历史残留或仅本地可清理的文件/目录。
2. 给出每个候选的判断依据，而不是只按名称主观猜测。
3. 区分“可直接删除”和“按需删除/保留”。

### 任务范围

1. 顶层目录与显著可疑文件。
2. 与候选路径相关的脚本、测试、文档、规则与 git 跟踪状态。

### 非目标

1. 不执行真实删除。
2. 不重构业务代码。
3. 不对全仓所有子文件逐个做死代码级审计。

## 3. 证据编号表

| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | 根目录结构与 `.gitignore` | 2026-04-18 21:49 | 初步锁定本地缓存、导出文件与历史目录候选 | Codex |
| E2 | `git ls-files` 与 `git status --short` | 2026-04-18 21:49 | 区分已跟踪文件与本地产物 | Codex |
| E3 | `rg` 全局检索候选路径引用 | 2026-04-18 21:51 | 区分真实被引用与仅历史保留 | Codex |
| E4 | `desktop_tests/flaui/README.md` 与 `docs/AGENTS/10-执行总则.md` | 2026-04-18 21:52 | 证明 `desktop_tests/flaui/` 为历史保留 fallback，而非默认主线 | Codex |
| E5 | `git check-ignore -v`、`git worktree list --porcelain`、`git ls-files` | 2026-04-18 22:00 | 证明缓存/本地产物忽略边界、`.worktrees` 失效状态与若干已跟踪噪音文件 | Codex |
| E6 | 顶层体积盘点与候选路径二次检索 | 2026-04-18 21:57-22:00 | 形成可删/按需删/保留三级清单，并确认清理优先级 | Codex |

## 4. 指挥拆解结果

| 序号 | 原子任务 | 目标 | 执行子 agent | 验证子 agent | 验收标准 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 候选路径盘点 | 识别可疑目录与文件 | 降级为主会话执行 | 降级为主会话复核 | 候选清单完整覆盖顶层显著项 | 已完成 |
| 2 | 引用关系核对 | 判断是否仍被脚本/文档/测试引用 | 降级为主会话执行 | 降级为主会话复核 | 每项候选都有引用结论 | 已完成 |
| 3 | 结果分级 | 形成可删/按需删/保留结论 | 降级为主会话执行 | 降级为主会话复核 | 输出清晰分级与风险提示 | 已完成 |

## 5. 子 agent 输出摘要

- 调研摘要：无子 agent；由主会话直接完成。
- 执行摘要：
  - 已读取全部 AGENTS 分册与相关技能约束。
  - 已检查 `.gitignore`、`git ls-files`、根目录结构与候选路径引用。
- 验证摘要：
  - 已用 `git check-ignore -v` 复核 `.pytest_cache`、`.serena`、`.tmp_runtime`、`.venv`、`.worktrees`、`dump.rdb` 的忽略状态。
  - 已用 `git worktree list --porcelain` 复核 `.worktrees/backend-p95-40-production-craft-phase1` 为 `prunable` 且指向不存在 gitdir 的失效 worktree。
  - 已用 `git ls-files` 复核 `backend/pyright_errors.txt`、`backend/remaining_errors.txt`、`frontend/query-export.csv`、`frontend/version-export.csv`、`tools/apply_docker_desktop_cn.ps1`、`tools/patch_docker_desktop_cn_overlay.ps1` 均为已跟踪文件。

## 6. 失败重试记录

| 轮次 | 原子任务 | 失败现象 | 根因判断 | 修复动作 | 复检结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 模板定位 | `docs/commander/*.md` 路径不存在 | 历史兼容跳转文件指向未检出目录 | 改用 `docs/AGENTS/50-模板与索引.md` 定义的结构自行留痕 | 已绕过 |

## 7. 工具降级、硬阻塞与限制

- 默认主线工具：`Sequential Thinking`、`update_plan`、PowerShell、`rg`
- 不可用工具：无
- 降级原因：子 agent 派发受更高优先级指令限制，本轮不使用
- 替代流程：由主会话完成分析，随后基于已执行命令与结果做独立复核
- 影响范围：缺少天然角色分离，但不影响静态审计结论形成
- 补偿措施：所有结论附路径引用与 git/ignore 证据
- 硬阻塞：无

## 8. 交付判断

- 已完成项：
  - 规则读取
  - 任务拆解
  - 候选路径初筛
  - 候选路径引用核对
  - 最终分级清单
  - 结果复核回填
- 未完成项：无
- 是否满足任务目标：是
- 主 agent 最终结论：可交付

## 8.1 最终分级清单

### A. 高置信“可直接删除”候选

1. `.pytest_cache/`
   - 依据：被 `.gitignore` 明确忽略；仅为 pytest 缓存；无业务代码依赖。
2. `dump.rdb`
   - 依据：被 `.gitignore` 明确忽略；已有历史 evidence 明确说明其为 Redis RDB 本地快照，不是源码文件。
3. `backend/pyright_errors.txt`
   - 依据：被 `.gitignore` 明确标记为本地错误导出；当前仍被 git 跟踪；仓库内未见真实消费链路。
4. `backend/remaining_errors.txt`
   - 依据：同上，属于历史调试残留文件。
5. `frontend/query-export.csv`
   - 依据：被 `.gitignore` 明确标记为本地导出噪音；当前文件为 0 字节；仓库内仅有另一台机器绝对路径字符串引用。
6. `frontend/version-export.csv`
   - 依据：同上，属于 0 字节导出占位残留。

### B. 高价值“应优先清理但先确认”的候选

1. `.worktrees/backend-p95-40-production-craft-phase1`
   - 依据：顶层体积约 `38.57 MB`；`git worktree list --porcelain` 显示其为 `prunable gitdir file points to non-existent location`；说明这是失效 worktree 残留，而不是当前正常挂载的工作树。
   - 备注：如要清理，建议按 git worktree 口径处理，而不是直接手工删目录。
2. `.serena/`
   - 依据：被 `.gitignore` 明确忽略；内容为本地索引/记忆与本地覆盖配置，不参与项目运行。
   - 备注：若当前不用 Serena，可删；后续工具再次使用时会重建。

### C. 非主线保留 / 按需删除候选

1. `desktop_tests/flaui/`
   - 依据：`docs/AGENTS/10-执行总则.md` 与目录自带 `README.md` 均明确其为“历史保留/按需 fallback”，不是默认测试主线。
   - 结论：它不是主线必需目录，但也不是纯死目录；只有在团队确认以后不再保留桌面系统级自动化 fallback 时，才建议删除。
2. `tools/apply_docker_desktop_cn.ps1`
3. `tools/patch_docker_desktop_cn_overlay.ps1`
   - 依据：两者仅服务于 Docker Desktop 汉化脚本场景，且脚本中写死了另一份仓库绝对路径 `C:\\Users\\Donki\\UserData\\Code\\ZYKJ_MES\\...`，不参与 MES 主线运行或测试。
   - 结论：若此仓库仅承载 MES 项目本身，这两份脚本可视为低风险清理候选；若你仍把仓库当个人运维工具箱，则保留。

### D. 不建议判为“无用”，但内容可清理

1. `.venv/`
   - 依据：`start_backend.py`、`backend/README.md`、多份计划文档都显式依赖它。
   - 结论：不是无用目录，但可在不需要当前环境时重建。
2. `.tmp_runtime/`
   - 依据：后端测试、样本脚本、性能文档、缓存标记都显式使用该路径。
   - 结论：目录本身有用，但里面的日志/临时 JSON/缓存内容可按需清空。

## 9. 迁移说明

- 无迁移，直接替换
