# 指挥官执行留痕：生产侧首件富表单改造（2026-04-03）

## 1. 任务信息

- 任务名称：生产侧首件富表单改造
- 执行日期：2026-04-03
- 执行方式：指挥官模式拆解调度 + 执行子 agent + 独立验证子 agent
- 当前状态：进行中

## 2. 输入来源

- 用户要求：当前首件页面过于简陋，至少达到参考图中的功能水平。
- 用户确认的业务规则：
  1. 保留现有入口，从生产订单查询/详情中的“首件”进入。
  2. 升级为完整首件录入，不再使用仅输入校验码的小弹窗。
  3. 本轮表单字段需真实落库。
  4. 工序锁定当前工序。
  5. 首件结果仅做“合格/不合格”。
  6. 首件内容、首件测试值先做单组输入。
  7. 本轮需要：首件模板、查看参数、添加操作员。
  8. 本轮不做：通知品质、模板管理页、多条检验项动态增删。
  9. 首件模板本轮由后台模板表 + 种子/数据库初始化维护。
  10. 沿用现有质量模块“每日首件/首件处置”链路。
  11. 添加操作员支持多人，候选范围为所有启用用户。

## 3. 工具降级记录

- Sequential Thinking 工具状态：当前会话不可用。
- 降级原因：工具链未提供对应入口。
- 替代措施：以显式书面拆解 + TodoWrite + evidence 留痕 + 子 agent 闭环完成等效分析。
- 影响范围：仅影响形式化思考留痕，不影响本次实现与验证闭环。

## 4. 现状事实

### 4.1 当前生产侧首件入口

- 文件：`frontend/lib/pages/production_order_query_page.dart`
- 当前仅通过 `_showFirstArticleDialog()` 弹出一个输入“当日校验码”的简陋对话框。
- 当前提交调用：`ProductionService.submitFirstArticle(...)`

### 4.2 当前生产侧首件提交契约

- 前端：`frontend/lib/services/production_service.dart`
- 后端：`backend/app/api/v1/endpoints/production.py`
- 当前字段仅有：
  - `order_process_id`
  - `pipeline_instance_id`
  - `verification_code`
  - `remark`
  - `effective_operator_user_id`
  - `assist_authorization_id`

### 4.3 当前首件记录模型

- 模型文件：`backend/app/models/first_article_record.py`
- 当前字段仅有：
  - `order_id`
  - `order_process_id`
  - `operator_user_id`
  - `verification_date`
  - `verification_code`
  - `result`
  - `remark`

### 4.4 可复用能力

- 质量模块已有：
  - 每日首件列表：`frontend/lib/pages/daily_first_article_page.dart`
  - 首件详情/处置：`frontend/lib/pages/first_article_disposition_page.dart`
  - 质量接口：`frontend/lib/services/quality_service.dart`
- 参数查看已有产品参数查询能力，可考虑在首件页内弹窗复用参数展示逻辑。
- 消息系统已有首件失败自动同步消息能力，但本轮用户明确不做“通知品质”。

## 5. 原子任务拆分与验收标准

### 任务 FA1：后端基础与契约扩展

- 目标：
  1. 扩展首件记录模型以支持模板、检验内容、测试值、参与操作员。
  2. 新增最小首件模板主数据能力（按产品 + 当前工序选模板）。
  3. 扩展生产侧首件提交契约。
  4. 补齐生产侧查看参数所需只读数据支撑。
  5. 让质量模块详情接口能返回新增字段。
- 验收标准：
  1. 存在 Alembic 迁移。
  2. 生产提交首件接口可接收并真实保存新增字段。
  3. 可按产品/工序查询可用模板。
  4. 可获取首件页面所需参数只读数据。
  5. 质量详情接口能读到新增字段。

### 任务 FA2：生产侧首件富表单页面

- 目标：
  1. 将简陋弹窗升级为完整首件录入表单。
  2. 页面包含：基础信息、模板、查看参数、添加操作员、首件内容、首件测试值、首件结果、首件检验码。
  3. 工序锁定当前工序。
- 验收标准：
  1. 不再出现仅校验码输入的小弹窗。
  2. 模板选择可带出内容/测试值。
  3. 参数可在页内弹窗查看。
  4. 可添加多个参与操作员。
  5. 提交时新增字段被带入请求体。

### 任务 FA3：质量侧详情联动

- 目标：
  1. 每日首件详情/处置页展示生产侧新增字段。
  2. 保持原有处置链路可用。
- 验收标准：
  1. 质量侧详情页可见模板、首件内容、首件测试值、参与操作员。
  2. 原有处置流程不回归。

## 6. 指挥官编排

1. 先做 FA1，后端契约稳定后再做 FA2。
2. FA2 完成后再做 FA3，避免质量详情页先改空字段。
3. 每个任务均按“执行子 agent -> 独立验证子 agent”闭环推进。

## 7. 执行记录

- 2026-04-03：任务启动，进入指挥官执行阶段，准备派发 FA1。
- 2026-04-03：FA1 执行完成。后端已补首件模板表、参与操作员关联表、首件富表单提交契约、模板查询/参与人查询/参数查看接口，以及质量详情新增字段返回；定向 `pytest` 与 `compileall` 通过，待独立验证。
- 2026-04-03：FA1 独立验证结论为 `FLAG`。
  1. `FirstArticleParticipant.user_id` 模型未声明 `index=True`，与迁移脚本存在 schema drift 风险。
  2. 迁移脚本尚未执行真实 `alembic upgrade` 验证，当前放行证据不足。
  下一步：返工修复索引声明并执行迁移实跑后重新独立验证。
- 2026-04-03：FA1.2 收敛完成。`FirstArticleParticipant.user_id` 已补 `index=True`，`alembic current/heads/upgrade head` 已实跑确认数据库位于 `x1y2z3a4b5c6 (head)`，并已核验 `mes_first_article_participant` 索引存在；定向回归继续通过，待重新独立验证。
- 2026-04-03：FA1/FA1.2 独立复核结论：`PASS`。后端模型、迁移、模板/参与人/参数接口与质量详情联动已达放行标准，允许进入 FA2。
- 2026-04-03：FA2 执行完成。生产侧原首件小弹窗已替换为独立首件录入页，模板、参数弹窗、多人参与操作员与富首件提交契约均已接入；目标前端 tests 与 `flutter analyze` 通过，待独立验证。
- 2026-04-03：FA2 独立验证结论：`PASS`。生产侧首件入口已切换为独立首件录入页，模板带出、参数弹窗、多人参与操作员与富请求体提交均已按验收点生效，允许进入 FA3。
- 2026-04-03：FA3 执行完成。质量侧首件详情/处置页已补展示模板名称、首件内容、首件测试值、参与操作员，原有处置提交流程与历史展示保持不变；目标前端 tests 与 `flutter analyze` 通过，待独立验证。
- 2026-04-03：FA3 独立验证结论：`PASS`。质量侧 `FirstArticleDetail` 已同步新增模板信息、首件内容、首件测试值、参与操作员，且首件详情模式、首件处置模式、原有处置提交流程与处置历史展示均未回归。

## 8. 最终结论

- 本轮“生产侧首件富表单改造”已完成并通过指挥官模式闭环验证。
- 已交付内容：
  1. 后端首件富表单模型、模板、参与操作员、提交契约、参数查询支撑。
  2. 生产侧独立首件录入页，支持模板带出、参数弹窗、多人参与、富字段提交。
  3. 质量侧每日首件详情/处置页联动显示新增字段。
- 当前明确未做：
  1. 通知品质按钮。
  2. 首件模板管理页。
  3. 多条动态检验项。
- 当前实施前置事项：
  1. 联调环境需已执行 Alembic 迁移至 `x1y2z3a4b5c6 (head)`。
  2. 若要真实演示模板选择，需要预置首件模板种子数据。
