# 指挥官执行留痕：指挥官流程工具化制度落地（2026-04-03）

## 1. 任务信息

- 任务名称：将指挥官工作流程与现有工具链制度化结合
- 执行日期：2026-04-03
- 执行方式：指挥官模式
- 当前状态：进行中
- 指挥模式：主 agent 拆解调度，子 agent 执行，独立子 agent 验证

## 2. 输入来源

- 用户指令：`可以，执行`
- 上游结论：当前仓库已经具备研究、环境、验证、治理四层工具能力，下一步最有价值的工作是把其制度化为任务分类、工具矩阵、默认触发规则、验证门禁与 evidence 模板。
- 相关基线：
  - `指挥官工作流程.md`
  - `evidence/指挥官任务日志模板.md`
  - `docs/opencode_tooling_bundle.md`
  - `docs/host_tooling_bundle.md`

## 3. 任务目标、范围与非目标

### 3.1 任务目标

1. 在仓库中落地可复用的“任务分类表、角色-工具矩阵、默认触发规则、验证门禁清单”。
2. 补充一份与现有指挥官流程兼容的 evidence 模板，便于后续按工具化方式留痕。
3. 保持最小正确边界，不与现有流程文件重复冲突。

### 3.2 任务范围

1. `docs/` 下的制度文档
2. `evidence/` 下的模板文件
3. 与上述文件必要相关的引用更新

### 3.3 非目标

1. 不改动现有业务代码
2. 不新增复杂的 `.opencode/commands/`、agent 配置或自动化脚本
3. 不重写现有 `指挥官工作流程.md`，仅做兼容补充

## 4. 初步验收标准

1. 仓库中至少新增 1 份制度文档与 1 份 evidence 模板。
2. 制度文档必须显式覆盖以下五项：
   - 任务分类表
   - 角色-工具矩阵
   - 默认触发规则
   - 验证门禁清单
   - evidence 输出要求
3. evidence 模板能直接复用，且与现有 `指挥官任务日志模板.md` 风格兼容。
4. 独立验证需确认内容完整、路径正确、与现有流程文件无明显冲突。

## 5. 子 agent 输出摘要

### 5.1 调研子 agent 结论

- 建议新增两份最小文件：
  - `docs/commander_tooling_governance.md`
  - `evidence/指挥官工具化验证模板.md`
- 建议仅做最小引用更新，避免重写 `指挥官工作流程.md` 与 `evidence/指挥官任务日志模板.md`。
- 建议任务分类至少覆盖 6 类，并与当前已装工具和项目脚本绑定。

### 5.2 实现子 agent 结果

- 已新增 `docs/commander_tooling_governance.md`
- 已新增 `evidence/指挥官工具化验证模板.md`
- 已对 `指挥官工作流程.md` 增加一处最小引用，指向新的治理文档与验证模板

## 6. 实际改动

- `docs/commander_tooling_governance.md`：新增指挥官流程工具化治理补充，覆盖任务分类表、角色-工具矩阵、默认触发规则、验证门禁清单、工具降级与代偿规则、evidence 输出要求与最小落地示例
- `evidence/指挥官工具化验证模板.md`：新增工具触发、执行、验证、失败重试、降级阻塞与收口闭环模板
- `指挥官工作流程.md`：新增一处引用，明确工具触发、验证门禁、降级代偿与补充留痕应遵循新增治理文档与模板

## 7. 验证结果

### 7.1 独立验证结论

- 结论：通过

### 7.2 关键验证点

1. 已满足“至少 1 份制度文档 + 1 份 evidence 模板”。
2. 制度文档显式覆盖：
   - 任务分类表
   - 角色-工具矩阵
   - 默认触发规则
   - 验证门禁清单
   - evidence 输出要求
3. evidence 模板可体现“工具触发 -> 执行 -> 验证 -> 重试 -> 收口”闭环。
4. 新增文档与 `指挥官工作流程.md`、`evidence/指挥官任务日志模板.md` 为补充关系，不构成角色或流程基线重复。
5. 文档引用的已有路径均已核实存在。

## 8. 限制与维护注意事项

- `docs/host_tooling_bundle.md` 记录了较多宿主机绝对路径，若未来换机或工具重装，需要同步刷新。
- `docs/opencode_tooling_bundle.md` 中 `frontend/integration_test` 采用条件式描述，若后续新增该目录，建议同步补齐对应验证约束。

## 9. 最终结论

- 本轮已将“任务分类表、角色-工具矩阵、默认触发规则、验证门禁清单、evidence 模板”正式落地到仓库。
- 当前指挥官模式已不只定义流程，还具备可复用的工具化治理规范。
- 无迁移，直接补充。
