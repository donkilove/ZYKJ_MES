# 任务日志：AGENTS 文档移除 MCP_DOCKER 强绑定

- 日期：2026-04-13
- 执行人：Codex 主 agent
- 当前状态：已完成
- 指挥模式：未触发；单轮文档规则整改

## 1. 输入来源

- 用户指令：`MCP_DOCKER` 服务已弃用，需要更新 `docs/AGENTS` 中的文档。
- 需求基线：
  - `AGENTS.md`
  - `docs/AGENTS/10-执行总则.md`
  - `docs/AGENTS/30-工具治理与验证门禁.md`
  - `docs/AGENTS/50-模板与索引.md`
- 代码范围：
  - `AGENTS.md`
  - `docs/AGENTS/*.md`
  - `evidence/`

## 1.1 前置说明

- 默认主线工具：当前会话实际可用的宿主工具与 MCP 服务、`Sequential Thinking`、`update_plan`
- 缺失工具：名为 `MCP_DOCKER` 的统一宿主入口
- 缺失/降级原因：项目规则仍保留旧口径，但当前环境已不再以 `MCP_DOCKER` 作为统一服务名暴露
- 替代工具：当前会话真实可用的 MCP 服务、宿主文件工具、宿主安全命令
- 影响范围：需要统一调整规则文案、分类矩阵、降级模板与日志模板，避免后续规则与真实环境冲突

## 2. 任务目标、范围与非目标

### 任务目标

1. 移除 `docs/AGENTS` 中对 `MCP_DOCKER` 的强制主线绑定。
2. 将规则改为“优先使用当前会话实际可用的宿主工具/MCP 服务”。
3. 保持前置说明、降级留痕、验证门禁与模板口径自洽。

### 任务范围

1. 根入口与分册正文中的规则口径调整。
2. 模板中关于默认主线工具、降级说明的占位文本调整。
3. 本轮 evidence 留痕。

### 非目标

1. 不批量回写历史 evidence。
2. 不调整业务代码或测试。

## 3. 证据编号表

| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | 根 `AGENTS.md` 与 `docs/AGENTS/*.md` 实读 | 2026-04-13 | 当前规则仍存在大量 `MCP_DOCKER` 强绑定 | Codex |
| E2 | `rg` 全文检索结果 | 2026-04-13 | 受影响核心文件为根入口、10、30、50 分册 | Codex |
| E3 | 本轮文档 diff 与复核检索 | 2026-04-13 | 目标文件已改为当前会话工具口径，且无关键 `MCP_DOCKER` 主线残留 | Codex |

## 4. 执行步骤

| 序号 | 步骤 | 目标 | 状态 |
| --- | --- | --- | --- |
| 1 | 读取规则与搜索旧口径 | 确认影响面 | 已完成 |
| 2 | 修改规则文件 | 完成新口径替换 | 已完成 |
| 3 | 复核遗留与收口 | 确认无关键漏项 | 已完成 |

## 6. 变更摘要

- 根 `AGENTS.md`：移除“默认强制优先使用 `MCP_DOCKER`”与“必须完成 `MCP_DOCKER Sequential Thinking`”表述，改为基于当前会话实际可用工具的中性规则。
- `docs/AGENTS/10-执行总则.md`：同步更新前置说明与任务拆解硬约束，避免与根入口冲突。
- `docs/AGENTS/30-工具治理与验证门禁.md`：将默认工具清单、分类矩阵与降级规则从指定服务名改为能力分组口径。
- `docs/AGENTS/50-模板与索引.md`：更新模板占位语，防止后续新 evidence 继续沿用旧口径。

## 7. 最终结论

- `MCP_DOCKER` 已从规则主线口径中移除，不再被要求为默认唯一入口。
- 新规则改为：优先使用当前会话实际可用的宿主工具与 MCP 服务，并保留缺失、降级、影响范围与残余风险的留痕要求。
- 本轮未回写历史 evidence；历史记录保留原语境。

## 5. 迁移说明

- 无迁移，直接替换
