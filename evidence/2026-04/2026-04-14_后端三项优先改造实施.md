# 任务日志：后端三项优先改造实施

- 日期：2026-04-14
- 执行人：Codex
- 当前状态：已完成
- 指挥模式：未触发；当前会话直接执行

## 1. 输入来源
- 用户指令：今晚开始做三件事，并允许直接清理当前本地工作区后继续实现。
- 需求基线：`docs/superpowers/specs/2026-04-14-backend-priority-fixes-design.md`、`docs/superpowers/plans/2026-04-14-backend-priority-fixes.md`
- 代码范围：`backend/` 及相关测试

## 1.1 前置说明
- 默认主线工具：`update_plan`、`Sequential Thinking`、`shell_command`、`Filesystem`、`apply_patch`
- 缺失工具：无
- 缺失/降级原因：无
- 替代工具：无
- 影响范围：无

## 2. 开始记录
- 开始时间：2026-04-14
- 当前阶段：实现准备
- 已执行工作区清理：
  - `git reset --hard HEAD`
  - `git clean -fd`
  - 删除失败分支 `backend-priority-fixes-20260414`
- 当前仓库口径：已恢复到干净状态后重新进入本轮实现

## 3. 证据编号表
| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | 用户当前指令 | 2026-04-14 | 本轮允许直接清理当前本地工作区后继续实现 | Codex |
| E2 | `git reset --hard HEAD` / `git clean -fd` 结果 | 2026-04-14 | 当前工作区已清理 | Codex |
| E3 | 本轮 spec 与 plan | 2026-04-14 | 实现范围锁定为三项优先改造 | Codex |

## 4. 当前进展
- 已恢复 spec / plan / evidence
- 已完成 Task 1：安全硬化
- 已完成 Task 2：事务边界统一
- 已完成 Task 3：`authz` 首批拆分

## 5. 实际改动
### 5.1 安全硬化
- 在 `backend/app/core/config.py` 新增运行态安全校验与危险默认值判断。
- 在 `backend/app/core/security.py` 为 token 签发与解码增加 JWT 密钥安全校验。
- 在 `backend/app/main.py` 与 `backend/app/worker_main.py` 启动入口增加运行态安全校验。
- 在 `backend/app/bootstrap/startup_bootstrap.py` 为引导管理员口令增加 fail-fast。
- 在 `backend/app/services/production_execution_service.py` 阻止弱默认首件验证码落库。
- 在 `backend/app/services/quality_service.py` 阻止查询侧暴露弱默认验证码。

### 5.2 事务边界统一
- `backend/app/services/product_service.py` 删除内部 `commit()`。
- `backend/app/services/role_service.py` 删除内部 `commit()`。
- `backend/app/api/v1/endpoints/products.py` 改为写审计后统一 `commit()`，异常时统一 `rollback()`。
- `backend/app/api/v1/endpoints/roles.py` 改为写审计后统一 `commit()`，异常时统一 `rollback()`。

### 5.3 authz 首批拆分
- 新增 `backend/app/services/authz_cache_service.py`，承接缓存 key / TTL 相关逻辑。
- 新增 `backend/app/services/authz_query_service.py`，承接 effective permission 计算相关逻辑。
- `backend/app/services/authz_service.py` 改为通过薄包装调用新模块，保持外部接口兼容。

### 5.4 测试补充
- 新增 `backend/tests/test_production_execution_service_unit.py`
- 新增 `backend/tests/test_startup_bootstrap_unit.py`
- 新增 `backend/tests/test_transaction_boundary_unit.py`
- 新增 `backend/tests/test_authz_split_unit.py`
- 扩展 `backend/tests/test_security_unit.py`
- 扩展 `backend/tests/test_app_startup_worker_split.py`
- 扩展 `backend/tests/test_quality_service_stats_unit.py`

## 6. 验证记录
- 建立本地虚拟环境与依赖：
  - `python -m venv .venv`
  - `.\\.venv\\Scripts\\python.exe -m pip install --upgrade pip`
  - `.\\.venv\\Scripts\\python.exe -m pip install -r backend\\requirements.txt`
- Task 1 红灯验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_security_unit backend.tests.test_app_startup_worker_split backend.tests.test_production_execution_service_unit backend.tests.test_startup_bootstrap_unit backend.tests.test_quality_service_stats_unit`
  - 结果：先失败，失败点覆盖 JWT、启动引导、验证码落库与查询回退
- Task 1 绿灯验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_security_unit backend.tests.test_app_startup_worker_split backend.tests.test_production_execution_service_unit backend.tests.test_startup_bootstrap_unit backend.tests.test_quality_service_stats_unit`
  - 结果：通过
- Task 2 红灯验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_transaction_boundary_unit`
  - 结果：先失败，体现 service 内部提交与 API 未回滚
- Task 2 绿灯验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_transaction_boundary_unit`
  - 结果：通过
- Task 3 红灯验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_authz_split_unit`
  - 结果：先失败，因新模块尚不存在
- Task 3 绿灯验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_authz_split_unit`
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_authz_service_unit`
  - 结果：通过
- 本轮统一验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_security_unit backend.tests.test_app_startup_worker_split backend.tests.test_production_execution_service_unit backend.tests.test_startup_bootstrap_unit backend.tests.test_quality_service_stats_unit backend.tests.test_transaction_boundary_unit backend.tests.test_authz_split_unit backend.tests.test_authz_service_unit backend.tests.test_authz_catalog_unit backend.tests.test_authz_endpoint_unit`
  - 结果：`Ran 42 tests ... OK`

## 7. 残余风险与边界
- 当前验证以本轮直接相关单测为主，没有额外跑更大范围的后端集成测试。
- `authz` 相关单测通过，但在 Redis 不可达时会输出回退警告；当前行为是可用的本地缓存降级，不影响测试通过。
- 本轮新增了仓库本地 `.venv` 作为执行环境，但它处于 `.gitignore` 管控范围内，不会进入版本库。

## 8. 最终结论
- 已完成项：
  1. 安全硬化与危险默认值 fail-fast。
  2. 产品/角色链路事务边界统一。
  3. `authz` 首批拆分。
  4. 真实验证命令执行与通过留痕。
- 未完成项：
  - 更大范围的后端集成测试未在本轮执行。
- 是否满足本轮目标：是
- 最终结论：可交付

## 8.1 下一步建议
- 第一优先：补一轮后端集成回归，优先覆盖登录、启动引导、产品创建、角色创建、首件查询、authz 接口。
- 第二优先：继续扩大“事务边界统一”范围，扫描其余 service 已提交、API 再提交的链路。
- 第三优先：继续做 `authz_service.py` 第二刀拆分，优先抽离 revision / read cache 与角色配置写入路径。

## 8.2 后续执行：1、2、3
### A. Docker 口径后端集成回归
- 用户补充约束：后端应按 Docker 部署口径验证，本机 5432 上已有宿主 PostgreSQL，禁止停掉。
- 根因定位结果：
  1. `compose.yml` 原始 PostgreSQL 宿主端口固定为 `5432`，与本机 `postgres` 冲突。
  2. `docker/web-entrypoint.sh`、`docker/worker-entrypoint.sh` 使用 CRLF 行尾，导致容器内 `env: 'sh\\r'` 启动失败。
  3. 由于本轮新增了运行态安全校验，Docker 环境还需要显式注入 `JWT_SECRET_KEY`、`BOOTSTRAP_ADMIN_PASSWORD`、`PRODUCTION_DEFAULT_VERIFICATION_CODE`。
- 实际修正：
  - `compose.yml` 改为支持 `POSTGRES_HOST_PORT`、`BACKEND_WEB_HOST_PORT`
  - `compose.yml` 将三项安全配置透传给 `backend-web` / `backend-worker`
  - `docker/web-entrypoint.sh`、`docker/worker-entrypoint.sh` 统一为 LF 行尾
- Docker 启动口径：
  - `POSTGRES_HOST_PORT=5433`
  - `BACKEND_WEB_HOST_PORT=8000`
  - `JWT_SECRET_KEY=docker-local-jwt-secret-20260414`
  - `BOOTSTRAP_ADMIN_PASSWORD=Admin_Local_20260414!`
  - `PRODUCTION_DEFAULT_VERIFICATION_CODE=FA20260414`
- Docker 烟测结果：
  - `/health`：200
  - `/api/v1/auth/login`：200
  - `/api/v1/authz/capability-packs/catalog?module=user`：200
  - `/api/v1/roles`：201
  - `/api/v1/users`：201
  - `/api/v1/products`：201

### B. 事务边界统一扩展
- 本轮新增统一链路：
  - `backend/app/services/user_service.py:create_user`
  - `backend/app/api/v1/endpoints/users.py:create_user_api`
  - `backend/app/services/assist_authorization_service.py:create_assist_authorization`
  - `backend/app/api/v1/endpoints/production.py:create_assist_authorization_api`
- 统一口径：
  - service 负责业务对象构建与 `flush()`
  - API 负责最终 `commit()/rollback()`
- 追加测试：
  - `test_create_user_service_does_not_commit_transaction`
  - `test_create_user_api_rolls_back_when_audit_log_fails`
  - `test_create_assist_authorization_service_does_not_commit_transaction`
  - `test_create_assist_authorization_api_commits_once`

### C. authz 第二刀拆分
- 新增文件：
  - `backend/app/services/authz_read_service.py`
- 本轮抽出职责：
  - 读缓存 `get/set/get_or_build`
  - revision token 组装 `build_authz_read_revision_state`
- `backend/app/services/authz_service.py` 保留薄包装调用，外部接口未变。
- 追加测试：
  - `test_build_authz_read_revision_state_is_stable`

## 8.3 下一步建议（本轮完成后）
- 第一优先：补跑更重的 Docker 业务集成回归，优先覆盖首件提交流程、生产订单主链路、用户权限批量应用与导出链路。
- 第二优先：继续扩大事务边界统一范围，重点扫描 `message`、`equipment`、`craft`、`quality` 中仍存在的 service/API 双层提交链路。
- 第三优先：继续拆 `authz_service.py` 写路径，优先抽离角色权限矩阵更新、能力包批量应用与 change log 记录逻辑。

## 8.4 后续执行：更深回归、事务继续扩展、authz 写路径拆分
### A. 更深的 Docker 业务集成回归
- 新增根因定位：
  - 本机 `5432` 已被宿主 PostgreSQL 占用，且用户明确禁止停掉。
  - 因此 Docker PostgreSQL 改走宿主 `5433`，容器内网络保持不变。
- 实际执行：
  - `docker compose up -d --build postgres redis backend-web backend-worker`
  - 容器环境显式注入：
    - `JWT_SECRET_KEY=docker-local-jwt-secret-20260414`
    - `BOOTSTRAP_ADMIN_PASSWORD=Admin_Local_20260414!`
    - `PRODUCTION_DEFAULT_VERIFICATION_CODE=FA20260414`
- 结果：
  - `backend-web` 健康
  - `backend-worker` 存活
  - Docker 业务烟测通过：
    - 登录
    - `authz` 能力包目录读取
    - 角色创建
    - 用户创建
    - 产品创建

### B. 事务边界继续扩展
- 新增统一链路：
  - `backend/app/services/user_service.py:create_user`
  - `backend/app/api/v1/endpoints/users.py:create_user_api`
  - `backend/app/services/assist_authorization_service.py:create_assist_authorization`
  - `backend/app/api/v1/endpoints/production.py:create_assist_authorization_api`
- 新增测试覆盖：
  - `test_create_user_service_does_not_commit_transaction`
  - `test_create_user_api_rolls_back_when_audit_log_fails`
  - `test_create_assist_authorization_service_does_not_commit_transaction`
  - `test_create_assist_authorization_api_commits_once`

### C. authz 写路径拆分
- 新增文件：
  - `backend/app/services/authz_write_service.py`
- 本轮抽出职责：
  - `_apply_role_permission_changes`
  - `_serialize_capability_pack_role_result`
  - `_record_capability_pack_change_log`
- `backend/app/services/authz_service.py` 保留薄包装调用，新旧接口兼容。
- 新增测试覆盖：
  - `test_apply_role_permission_changes_updates_existing_and_missing_rows`

## 8.5 追加验证记录
- 事务边界扩展验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_transaction_boundary_unit`
  - 结果：`Ran 8 tests ... OK`
- authz 写路径拆分验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_authz_split_unit`
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_authz_service_unit backend.tests.test_authz_catalog_unit backend.tests.test_authz_endpoint_unit`
  - 结果：均通过
- 本轮统一追加验证：
  - `.\\.venv\\Scripts\\python.exe -m unittest backend.tests.test_security_unit backend.tests.test_app_startup_worker_split backend.tests.test_production_execution_service_unit backend.tests.test_startup_bootstrap_unit backend.tests.test_quality_service_stats_unit backend.tests.test_transaction_boundary_unit backend.tests.test_authz_split_unit backend.tests.test_authz_service_unit backend.tests.test_authz_catalog_unit backend.tests.test_authz_endpoint_unit`
  - 结果：`Ran 47 tests ... OK`
- Docker 烟测：
  - `DOCKER_SMOKE_OK`

## 8.6 当前阶段下一步建议
- 第一优先：把当前 Docker 烟测固化成仓库内可重复执行的集成回归脚本或测试，而不是继续依赖临时命令。
- 第二优先：在这个自动化 Docker 回归里补上首件提交、生产订单主链路、导出链路三类长链路。
- 第三优先：等自动化回归落稳后，再继续清理剩余模块里的双层提交与 `authz` 写路径拆分。

## 9. 迁移说明
- 无迁移，直接替换
