# 任务日志：AGENTS 规则文档解耦改造

- 日期：2026-04-12
- 执行人：Codex 主 agent
- 当前状态：已完成
- 指挥模式：主 agent 执行实施与汇总，独立验证以同轮分阶段补偿方式完成

## 1. 输入来源
- 用户指令：按既定方案实施 AGENTS 规则文档解耦，将根 `AGENTS.md` 改为薄入口，并将正文拆分到 `docs/AGENTS/`。
- 需求基线：
  - `AGENTS.md`
  - `docs/`
  - `evidence/`
- 代码范围：
  - `AGENTS.md`
  - `docs/AGENTS/`
  - `evidence/task_log_20260412_agents_docs_decoupling.md`
  - `evidence/verification_20260412_agents_docs_decoupling.md`

## 1.1 前置说明
- 默认主线工具：`MCP_DOCKER Sequential Thinking`、`update_plan`、宿主安全命令、`apply_patch`
- 缺失工具：`rg` 不可用；本轮未启用子 agent；未切换 git worktree
- 缺失/降级原因：
  - `rg.exe` 在当前环境启动被拒绝访问，无法作为默认检索工具使用
  - 更高优先级工具约束要求仅在用户显式要求时才派发子 agent
  - 当前工作区已存在用户未提交改动与未跟踪规则文档，若切换 worktree 将脱离当前真实基线
- 替代工具：
  - 使用 PowerShell 原生命令替代 `rg`
  - 使用同轮分阶段独立验证替代验证子 agent
  - 在当前工作区最小改动执行，避免影响无关文件
- 影响范围：
  - 仅影响检索方式、验证组织方式与隔离策略
  - 不影响本轮文档拆分与验证闭环结论

## 2. 任务目标、范围与非目标
### 任务目标
1. 将根 `AGENTS.md` 改写为薄入口与强约束装配点。
2. 新增 `docs/AGENTS/` 六份正文分册并完成语义去重。
3. 为本轮改造补齐任务日志与验证日志。

### 任务范围
1. 允许修改 `AGENTS.md`。
2. 允许新增 `docs/AGENTS/*.md`。
3. 允许新增并回填本轮 `evidence/` 日志。

### 非目标
1. 不修改业务代码与测试逻辑。
2. 不批量回写历史 `evidence/` 路径。
3. 不处理与本轮无关的前端、MCP 或其他文档改动。

## 3. 证据编号表
| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | 用户确认实施既定拆分方案 | 2026-04-12 | 允许直接进入文档解耦实施 | 主 agent |
| E2 | 当前 `AGENTS.md` 内容盘点 | 2026-04-12 | 根文档当前仍为单文件高内聚规则源 | 主 agent |
| E3 | 当前工作区 `git status --short` | 2026-04-12 | 工作区存在用户未提交改动，本轮需最小改动执行 | 主 agent |
| E4 | 非 `evidence/` 活跃引用检索 | 2026-04-12 | 当前主要迁移对象为根 `AGENTS.md` 自身与新增正文分册 | 主 agent |
| E5 | 目标路径存在性校验 | 2026-04-12 | 根入口、六份分册与两份历史模板索引路径均存在 | 主 agent |
| E6 | 根 `AGENTS.md` 结构校验 | 2026-04-12 | 根入口保留 `apply: 始终`、标题正确，正文收敛为 45 行 | 主 agent |
| E7 | 旧口径与失效入口检索 | 2026-04-12 | 非 `evidence/` 路径中未检出 `唯一权威规则源`、`docs/指挥官文档`、`指挥官工作流程.md` 等旧入口残留 | 主 agent |
| E8 | 定向 `git status --short` | 2026-04-12 | 本轮文件已落地，但当前工作区仍存在用户既有的 `AGENTS.md` 索引异常状态 | 主 agent |

## 4. 指挥拆解结果
| 序号 | 原子任务 | 目标 | 执行子 agent | 验证子 agent | 验收标准 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 根入口改写 | 将根 `AGENTS.md` 收敛为薄入口与强约束装配点 | 主 agent | 同轮独立验证补偿 | 根文档保留 `apply: 始终`、优先级、硬约束与阅读顺序 | 已完成 |
| 2 | 正文分册拆分 | 新增 `docs/AGENTS/` 六份分册并完成章节映射 | 主 agent | 同轮独立验证补偿 | 原 12 个一级章节可映射到新结构且无遗漏 | 已完成 |
| 3 | 留痕与验证收口 | 完成验证日志与最终交付判断 | 主 agent | 同轮独立验证补偿 | evidence 闭环完整，迁移口径明确 | 已完成 |

## 5. 子 agent 输出摘要
- 调研摘要：已完成现状盘点、工作区状态核查与活跃引用检索。
- 执行摘要：
  - 将根 `AGENTS.md` 从 301 行完整正文收敛为 45 行薄入口。
  - 新增 `docs/AGENTS/00-50` 六份分册，按“导航、总则、工作流、工具治理、质量留痕、模板索引”拆分。
  - 清理旧的“单文件唯一正文/唯一权威规则源”口径，改为“根入口 + 分册正文”的单一规则集。
  - 将当前有效支撑索引改为实际存在的路径，不再把不存在的 `docs/opencode_tooling_bundle.md` 与 `docs/host_tooling_bundle.md` 作为有效入口。
- 验证摘要：
  - 目标文件存在性校验通过。
  - 根入口关键字段与行数校验通过。
  - 非 `evidence/` 旧入口残留检索通过。
  - 定向 `git status` 已记录当前工作区既有索引异常背景。

## 6. 失败重试记录
| 轮次 | 原子任务 | 失败现象 | 根因判断 | 修复动作 | 复检结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 检索准备 | `rg.exe` 无法启动 | 当前环境权限限制 | 改用 PowerShell 原生命令检索 | 已切换，待最终验证 |

## 7. 工具降级、硬阻塞与限制
- 默认 `MCP_DOCKER` 主线：`Sequential Thinking`
- 不可用工具：`rg`
- 降级原因：可执行文件启动被拒绝访问
- 替代流程：PowerShell 定向检索 + `apply_patch` 编辑
- 影响范围：检索效率下降，但不影响结论
- 补偿措施：在本日志与验证日志中明确记录
- 硬阻塞：无
- 已知限制：当前工作区在本轮开始前已存在 `AGENTS.md` 的索引删除/工作树新增状态，本轮不回退、不改写该既有索引历史，仅在工作树中完成新入口与分册落地

## 8. 交付判断
- 已完成项：
  - 实施前规则现状盘点
  - 活跃引用范围确认
  - 本轮 evidence 初稿建立
  - 根入口与正文分册改写
  - 结构、引用与旧口径真实验证
  - evidence 日志回填收口
- 未完成项：
  - 无
- 是否满足任务目标：是
- 主 agent 最终结论：可交付

## 9. 迁移说明
- 无数据迁移，直接替换。
