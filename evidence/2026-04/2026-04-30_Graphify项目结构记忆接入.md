# 任务日志：Graphify 项目结构记忆接入

- 日期：2026-04-30
- 执行人：Codex
- 当前状态：已完成
- 指挥模式：主 agent 拆解执行，阶段分离验证

## 1. 输入来源

- 用户指令：使用 Graphify 构建每个项目的记忆，并更新相关规则。
- 外部来源：Graphify 官网与官方 GitHub README。
- 需求基线：根 `AGENTS.md`、`docs/AGENTS/*.md`、当前会话 Memory MCP。
- 代码范围：规则文档、`.graphifyignore`、`.gitignore`、`graphify-out/`、`progress.md`。

## 1.1 前置说明

- 默认主线工具：宿主 PowerShell、`apply_patch`、`update_plan`、MCP_DOCKER Sequential Thinking、Memory MCP、Graphify。
- 缺失工具：全局 `graphify`、`uv`、`pipx`。
- 缺失/降级原因：当前环境未预装全局 Graphify；`uv`/`pipx` 未检出。
- 替代工具：使用项目本地 `.graphify-venv` 安装 `graphifyy==0.5.6` 并调用 `.graphify-venv\Scripts\graphify.exe`。
- 影响范围：不影响仓库运行时；`.graphify-venv/` 已加入 `.gitignore`，仅作为本地工具环境。
- 修复后复测：`.graphify-venv\Scripts\graphify.exe --help` 返回 Graphify 命令列表；`.graphify-venv\Scripts\graphify.exe update .` 成功生成代码结构图谱。

## 2. 任务目标、范围与非目标

### 任务目标

1. 将 Graphify 定义为项目级结构记忆层。
2. 配置 Graphify 输入边界，避免敏感与噪音文件进入图谱。
3. 更新 AGENTS 规则，明确 Graphify、Memory MCP、evidence/progress 的边界。
4. 生成首版代码结构图谱并验证可查询。

### 任务范围

1. 新增 `.graphifyignore`。
2. 更新 `.gitignore`，忽略 Graphify 本地虚拟环境和缓存/中间产物。
3. 更新根 `AGENTS.md` 与 `docs/AGENTS/10/30/40/50`。
4. 生成 `graphify-out/GRAPH_REPORT.md` 与 `graphify-out/graph.json`。
5. 写入 Memory MCP 的稳定项目记忆。

### 非目标

1. 不做全量文档/图片语义抽取。
2. 不运行 `graphify codex install` 自动改写规则入口。
3. 不提交密钥、环境文件、缓存、中间产物或本地虚拟环境。

## 3. 证据编号表

| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | Graphify 官网/官方 GitHub README | 2026-04-30 | Graphify 支持 Codex、输出 `GRAPH_REPORT.md`/`graph.json`/`graph.html`，`.graphifyignore` 使用 gitignore 风格 | Codex |
| E2 | `Get-Command graphify`、`python -m graphify --help` | 2026-04-30 23:xx | 全局 Graphify 不可用 | Codex |
| E3 | `.graphify-venv\Scripts\graphify.exe --help` | 2026-04-30 23:xx | 本地安装的 `graphifyy==0.5.6` 可用 | Codex |
| E4 | `.graphify-venv\Scripts\graphify.exe update .` | 2026-04-30 23:xx | 已生成代码结构图谱：7614 nodes、15401 edges；`graph.html` 因超过 5000 nodes 被跳过 | Codex |
| E5 | `graphify-out/GRAPH_REPORT.md`、`graphify-out/graph.json` | 2026-04-30 23:27 | 图谱报告与原始图谱已落地 | Codex |
| E6 | Memory MCP `read_graph` | 2026-04-30 23:xx | 已写入 Graphify/Memory 分层策略和运行约定 | Codex |
| E7 | `.graphify-venv\Scripts\graphify.exe query "message service todo dashboard" --budget 1200` | 2026-04-30 23:xx | 图谱查询可定位消息中心/待办相关节点 | Codex |
| E8 | `git diff --check`、`git status --short --untracked-files=all` | 2026-04-30 23:xx | 提交前空白检查通过，待提交范围符合本轮目标 | Codex |

## 4. 指挥拆解结果

| 序号 | 原子任务 | 目标 | 执行子 agent | 验证子 agent | 验收标准 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 核对官方用法 | 避免凭记忆写错命令 | 主 agent | 主 agent | 官方 README 与本地 help 对齐 | 完成 |
| 2 | 配置忽略边界 | 排除敏感与噪音输入 | 主 agent | 主 agent | `.graphifyignore` 覆盖密钥、env、缓存、构建产物、日志 | 完成 |
| 3 | 更新规则 | 明确 Graphify/Memory/evidence 边界 | 主 agent | 主 agent | 根入口短硬约束，工具治理分册细则 | 完成 |
| 4 | 生成图谱 | 产出项目结构记忆 | 主 agent | 主 agent | `GRAPH_REPORT.md` 与 `graph.json` 存在 | 完成 |
| 5 | 验证查询 | 验证图谱可用于结构导航 | 主 agent | 主 agent | `graphify query` 能返回相关节点 | 完成 |

## 5. 执行摘要

- 新增 `.graphifyignore`，将密钥、环境文件、构建产物、缓存、日志、临时目录、工具虚拟环境和任务流水排除在 Graphify 输入之外。
- `.gitignore` 新增 `.graphify-venv/` 与 Graphify 缓存/中间产物忽略规则。
- 根 `AGENTS.md` 新增 Graphify 存在时的优先读取硬约束。
- `docs/AGENTS/30-工具治理与验证门禁.md` 新增 Graphify 项目结构记忆口径、CAT-09 和降级规则。
- `docs/AGENTS/40/50` 补充 Graphify/Memory 留痕与模板字段。
- Memory MCP 写入两组稳定事实：`ZYKJ_MES 项目记忆策略`、`ZYKJ_MES Graphify 运行约定`。
- 生成代码结构图谱 `graphify-out/GRAPH_REPORT.md` 与 `graphify-out/graph.json`。

## 6. 失败重试记录

| 轮次 | 原子任务 | 失败现象 | 根因判断 | 修复动作 | 复检结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 检测本地 Graphify | `Get-Command graphify` 无输出，`python -m graphify --help` 报无模块 | 全局未安装 Graphify | 创建 `.graphify-venv` 并安装 `graphifyy` | `.graphify-venv\Scripts\graphify.exe --help` 成功 |
| 2 | 使用项目 `.venv` | `.venv\Scripts\python.exe` 不存在 | 当前 `.venv` 是类 Unix 结构 | 改用独立 `.graphify-venv`，避免污染后端虚拟环境 | 安装成功 |
| 3 | 全量检测 | 初始检测 1614 files、799 docs、5 images，语义抽取成本高 | 仓库文档较多，不适合本轮直接全量语义抽取 | 先执行 `graphify update .` 生成 AST-only 代码结构图谱 | 代码图谱生成成功 |
| 4 | 中文业务查询 | `graphify query "首页待办 消息中心"` 无匹配节点 | 当前图谱为代码结构图，中文业务语义未抽取 | 改用英文/代码名 `message service todo dashboard` | 查询返回消息待办相关测试节点 |

## 7. 工具降级、硬阻塞与限制

- 默认主线工具：Graphify、MCP_DOCKER、宿主 PowerShell、`apply_patch`。
- 不可用工具：全局 `graphify`、`uv`、`pipx`。
- 降级原因：当前环境未预装。
- 替代流程：项目本地 `.graphify-venv` 安装 `graphifyy==0.5.6`。
- 影响范围：图谱生成可用；无需全局安装；`.graphify-venv/` 不入库。
- 补偿措施：规则中明确 Graphify 作为辅助索引，源码/测试/契约仍为最终事实源。
- 硬阻塞：无。
- 限制：本轮只生成 AST-only 代码结构图谱，未做文档/图片语义抽取；`graph.html` 因节点超过 Graphify 5000 限制被跳过；Graphify 命令行与报告的 community 数量存在口径差异，已记录，不影响图谱文件可用性。

## 8. 交付判断

- 已完成项：Graphify 忽略配置、规则更新、Memory MCP 稳定事实写入、首版代码图谱生成、图谱查询验证、留痕更新。
- 未完成项：全量文档/图片语义图谱未生成，后续应按子目录或明确范围分批执行。
- 是否满足任务目标：是。
- 主 agent 最终结论：可交付。
- 提交前复核：`git diff --check` 退出码 0，仅 Git 行尾转换提示；`git status --short --untracked-files=all` 确认 `.graphify-venv/` 与 `graphify-out/cache/` 未进入待提交范围。

## 9. 迁移说明

- 无迁移，直接替换。
