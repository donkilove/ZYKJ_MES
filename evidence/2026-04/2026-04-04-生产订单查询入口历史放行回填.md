# 任务日志：生产订单查询入口补历史放行数据回填

## 基本信息
- 任务时间：2026-04-04
- 执行角色：执行子 agent
- 目标：在生产订单查询入口读取历史订单时，自动回填旧逻辑遗留的下工序 `visible_quantity` 与 `ProductionSubOrder` 可见性，不通过脚本或人工 SQL 直接修库。

## 计划
1. 定位 `production_order_service.py` 中查询入口与现有历史回填逻辑。
2. 复用现有 helper 与提交点，补充最小历史放行回填逻辑。
3. 补一条贴近 `my-orders` 用户现象的自动化测试。
4. 运行最小验证并记录结果。

## 证据
- 证据#1：`backend/app/services/production_order_service.py::_backfill_operator_sub_orders`
  - 结论：`own` 查询入口已存在历史子单补齐逻辑，并在 `_collect_my_order_items` 中执行后 `db.commit()`。
- 证据#2：`backend/app/services/production_execution_service.py::end_production`
  - 结论：当前未来逻辑已在报工完成时按前工序累计完成量提升下工序 `visible_quantity`，并调用 `ensure_sub_orders_visible_quantity` 同步子单。

## 降级记录
- 无。

## 进行中
- 已在 `backend/app/services/production_order_service.py` 中新增历史放行量回填 helper，并接入 `own` 查询入口已有 `_backfill_operator_sub_orders` 流程。
- 已在 `backend/tests/test_production_module_integration.py` 补充 `test_my_orders_backfills_historical_release_visibility_on_query`，覆盖历史脏数据在查询时自动恢复可见。

## 验证
- 命令：`python -m pytest backend/tests/test_production_module_integration.py -k "backfills_historical_release_visibility_on_query"`
  - 结果：通过，`1 passed, 24 deselected`。
- 命令：`python -m pytest backend/tests/test_production_module_integration.py -k "my_orders_contract_includes_supplier_due_date_and_remark or backfills_historical_release_visibility_on_query"`
  - 结果：通过，`2 passed, 23 deselected`。

## 结论
- 查询入口现在会在 `own` 视角读取前，按前工序累计完成量回填历史订单下工序 `visible_quantity`。
- 回填后继续复用 `ensure_sub_orders_visible_quantity` 同步 `ProductionSubOrder.assigned_quantity`、`is_visible` 与状态，并沿用原有 `db.commit()` 持久化。
