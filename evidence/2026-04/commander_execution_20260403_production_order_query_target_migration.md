# 指挥官执行留痕：生产订单查询目标项目迁移（2026-04-03）

## 1. 任务信息

- 任务名称：将 `SCGLXT` 的生产订单查询核心语义迁移到 `ZYKJ_MES`
- 执行日期：2026-04-03
- 当前状态：进行中
- 指挥模式：是
- 主目标：第一版与第二版合并实施，完成角色切换、订单可见性控制、行操作判定与前后端契约同步
- 本轮约束：保留真实后端分页、手动查询、公共列表容器 `CrudListTableSection` 与菜单式操作入口，不做 git 提交

## 2. 输入来源

- 用户指令：对照 `C:\Users\Donki\UserData\Code\SCGLXT` 中目标项目的生产订单查询页，将其功能语义迁移到本项目；本轮启用指挥官模式推进第一版与第二版一起做。
- 目标项目参考页：`SCGLXT_CGB_0.1.0/src/ui/son_page/production_order_query_page.py`
- 本项目目标页：`frontend/lib/pages/production_order_query_page.dart`
- 本项目后端主链路：
  - `backend/app/services/production_order_service.py`
  - `backend/app/api/v1/endpoints/production.py`
  - `backend/app/schemas/production.py`

## 3. 工具与流程降级记录

- 不可用工具：`Sequential Thinking`、`update_plan`、`Task`
- 触发时间：2026-04-03
- 降级原因：当前会话工具集中未提供对应入口
- 替代方案：
  - 以 `TodoWrite` 维护在制项
  - 以本文件记录等效拆解、执行闭环与验证闭环
  - 由于无 `Task` 子 agent，本轮采用“同会话分段执行并显式区分执行闭环/验证闭环”的补偿方式留痕
- 影响范围：子 agent 物理隔离能力缺失，需通过更严格的留痕、分阶段验证与结果对照补偿

## 4. 任务目标与非目标

### 4.1 任务目标

1. 将目标项目中的角色切换语义迁移到本项目，但以本项目现有 `own / assist / proxy` 语义重组，不直接照抄。
2. 将订单可见性控制迁移到本项目，包含：列表可见、管理员默认自可见、零可见数量隐藏。
3. 将首件、报工、代班、送修的菜单显隐收敛为行级规则，其中详情始终保留。
4. 保持真实分页、手动查询与现有业务入口不回退。

### 4.2 非目标

1. 不迁移目标项目的本地分页。
2. 不迁移目标项目的实时搜索。
3. 不改成目标项目的行内按钮主操作形式。
4. 不做 git 提交。

## 5. 用户已确认口径

1. 核心迁移点：角色切换、行动作判定、订单可见性控制。
2. 操作列形式：保留当前菜单式入口。
3. 搜索触发：继续手动查询。
4. 分页：保留本项目真实后端分页。
5. 角色切换：在现有语义上迁移，采用“角色 + 操作员”两级选择，且角色后必须选人。
6. 列表可见性：纳入迁移范围。
7. 管理员默认视角：改为自可见，默认空列表可接受。
8. 零可见数量：默认隐藏。

## 6. 当前验收标准

1. 页面筛选区完成“生产上下文 + 角色 + 操作员 + 状态 + 当前工序”的重组，其中代理视角需先选角色再选操作员。
2. 管理员 `own` 默认不再返回全量订单，而是遵循自可见规则。
3. `visible_quantity=0` 的记录默认不出现在列表中，除非业务规则明确要求保留。
4. `详情` 菜单项始终显示。
5. `首件`、`报工`、`代班`、`送修` 均受行级规则控制，不再只按页面权限粗放显示。
6. 真实分页、刷新保留当前页、查询回第一页行为不回退。

## 7. 原子任务拆分

### A. 后端契约与语义收敛

- A1：核对 `MyOrderItem` 当前字段，定义新增行级字段与角色上下文字段。
- A2：调整 `list_my_orders` / `_collect_my_order_items` 的管理员默认可见性与 `proxy` 语义。
- A3：补齐 `can_apply_assist`、`can_create_manual_repair` 等行级判定。
- A4：如有必要，扩展操作员选项接口以支持角色上下文筛选。

### B. 前端页面与契约同步

- B1：同步前端模型与 service 契约。
- B2：重构 `production_order_query_page.dart` 的筛选区状态与联动。
- B3：保留菜单式操作列，改为按后端行级字段显隐。
- B4：补最小 widget 回归，覆盖角色/操作员联动、菜单显隐、分页不回退。

### C. 验证闭环

- C1：后端静态校验与目标测试。
- C2：前端 widget test 与 `flutter analyze`。
- C3：更新本文件，形成执行摘要、验证结果、风险与结论。

## 8. 已收集证据（初始）

| 证据编号 | 来源 | 形成时间 | 适用结论 |
| --- | --- | --- | --- |
| E1 | `SCGLXT` 目标页与 `order_impl.py` 源码对照 | 2026-04-03 | 目标页核心是角色切换、可见性控制与行级动作判定 |
| E2 | 本项目 `production_order_query_page.dart` 与后端 `production_order_service.py` 对照 | 2026-04-03 | 本项目已有真实分页、首件/报工字段和部分可见性基础，但管理员默认仍是全量视角 |

## 9. 执行记录

- 00:00 主 agent 建立指挥官日志与在制项。
- 00:00 已装载前后端契约同步、后端联动、Flutter CRUD 页面技能。
- 00:00 下一步：收敛最终实现边界并进入执行闭环 A。
