# 任务日志：当前会话可用 MCP 工具与技能盘点

- 日期：2026-04-13
- 执行人：Codex 主 agent
- 当前状态：已完成
- 指挥模式：未触发；单轮只读查询

## 1. 用户目标

- 用户问题：你现在可用的 MCP 工具有哪些？有的技能是哪些？

## 2. 前置说明

- 默认规则要求优先使用 `MCP_DOCKER` 口径工具。
- 本轮未发现名称直接暴露为 `MCP_DOCKER` 的专用宿主工具入口。
- 降级原因：当前会话暴露的是实际注册后的多组 MCP 服务与宿主工具，而非统一 `MCP_DOCKER` 命名。
- 替代动作：使用 `list_mcp_resources`、`list_mcp_resource_templates`、会话工具定义与 Serena 当前配置交叉盘点。
- 影响范围：可准确回答“当前会话已注册并可调用的工具范围”，但不能把所有能力都标记为统一的 `MCP_DOCKER` 名称。
- 残余风险：部分 MCP 服务函数数量极大，最终回复将按服务分组列出，默认不逐个展开全部函数名；技能文件因工作区访问边界不可直接读取正文，只能依据会话已注入的技能注册清单回答。

## 3. 证据编号表

| 证据编号 | 来源 | 形成时间 | 适用结论 | 记录责任 |
| --- | --- | --- | --- | --- |
| E1 | `docs/AGENTS/00-导航与装配说明.md`、`docs/AGENTS/30-工具治理与验证门禁.md`、`docs/AGENTS/40-质量交付与留痕.md` | 2026-04-13 | 已完成本轮规则对齐，确认需说明 `MCP_DOCKER` 缺失降级 | Codex |
| E2 | `list_mcp_resources` | 2026-04-13 | 当前可见 MCP 资源来自 PostgreSQL 服务，已挂载大量表 schema 资源 | Codex |
| E3 | `list_mcp_resource_templates` | 2026-04-13 | 当前未暴露资源模板 | Codex |
| E4 | `get_current_config` | 2026-04-13 | Serena 项目工具当前可用，并可作为一类 MCP 能力对外说明 | Codex |
| E5 | 会话注入的 skills 清单 | 2026-04-13 | 当前可用技能可按名称、用途与路径对外说明 | Codex |

## 4. 执行步骤

| 序号 | 步骤 | 目标 | 状态 |
| --- | --- | --- | --- |
| 1 | 读取规则与历史留痕 | 确认本轮说明口径与 evidence 要求 | 已完成 |
| 2 | 盘点当前会话 MCP 资源 | 确认真实挂载情况 | 已完成 |
| 3 | 盘点当前会话技能清单 | 确认可触发技能范围 | 已完成 |
| 4 | 整理可用工具分组 | 面向用户输出清单 | 已完成 |

## 5. 迁移说明

- 无迁移，直接替换

## 6. 最终结论

- 当前会话可用的 MCP 能力主要包括：顺序思考、GitHub、Context7、PostgreSQL 查询、浏览器自动化、知识图谱、MES 业务 API、文件系统、Git、通用抓取与 Serena 项目级符号工具。
- 其中 `list_mcp_resources` 实际返回的资源仅明确显示 PostgreSQL schema 资源；其他服务当前体现为“工具可调用”，不一定都有独立资源模板。
- 当前会话还注入了多组 skills，可按“通用工作流类”“代码质量/验证类”“系统能力类”分组说明。
- 用户回复采用“按服务分组 + 代表能力”口径，不逐个罗列超大型 API 服务的全部函数名。
