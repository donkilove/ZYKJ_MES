# 任务日志：独立窗口插件架构可行性讨论

- 日期：2026-04-25
- 执行人：Codex
- 当前状态：进行中
- 指挥模式：本轮为架构讨论；当前会话未启用子 agent，采用“调研 -> 设计 -> 用户确认 -> 留痕”分阶段补偿

## 1. 输入来源

- 用户指令：
  - 那我要是让插件有独立窗口，不嵌进主程序是不是就不闪了，也能满足使用需求呢？
  - 窗口使用 PySide6 构建，后台也是 Python，MES 主程序只负责启动与关闭，怎么样？
- 需求基线：
  - `AGENTS.md`
  - `docs/AGENTS/00-导航与装配说明.md`
  - `docs/AGENTS/10-执行总则.md`
  - `docs/AGENTS/20-指挥官模式与工作流.md`
  - `docs/AGENTS/30-工具治理与验证门禁.md`
  - `docs/AGENTS/40-质量交付与留痕.md`
  - `docs/AGENTS/50-模板与索引.md`
- 排查范围：
  - 当前插件宿主与嵌入式 WebView 方案
  - 既有 `plugins/` 目录与 Python 运行时策略

## 1.1 前置说明

- 默认主线工具：`brainstorming`、`update_plan`、宿主安全命令（PowerShell / git）
- 缺失工具：独立 `Sequential Thinking`、结构化检索 MCP、浏览器验证入口、可用 `rg.exe`
- 缺失/降级原因：
  - 当前会话未提供独立 `Sequential Thinking`、结构化检索 MCP 与浏览器验证入口
  - `rg.exe` 在当前宿主路径启动时被拒绝访问
- 替代工具：
  - 使用 `update_plan` 做等效拆解
  - 使用 PowerShell 文本检索与既有设计/计划文件补偿

## 2. 当前判断

1. 用户正在重新评估插件 UI 承载形态。
2. 新方向是：插件使用独立窗口，窗口框架采用 PySide6，MES 主程序只负责启动与关闭。
3. 这一方向会显著改变我们前面围绕“主程序内嵌插件页面”的设计假设，需要先确认是否接受从“嵌入式插件”切到“外部工具中心”。
4. 用户已确认接受该取舍：插件窗口弹出到主程序外部，插件中心只保留“打开 / 关闭 / 运行状态 / 日志入口”等宿主能力。
5. 用户已确认继续推进该新方向，并要求开始收敛迁移方案。

## 3. 当前状态

- 已完成：
  - 记录新的架构讨论入口
- 进行中：
  - 评估 PySide6 独立窗口方案是否更适合当前需求

## 4. 迁移说明

- 无迁移，直接替换
