# 指挥官任务日志

## 1. 任务信息

- 任务名称：补充 Serena 用后关闭规则
- 执行日期：2026-04-07
- 执行方式：规则定位 + 文档最小改动 + 独立验证
- 当前状态：已完成
- 指挥模式：主 agent 拆解调度，执行子 agent 修改规则，独立验证子 agent 复核

## 2. 输入来源

- 用户指令：把“Serena 用完就关掉，不要开一堆不关”的要求写进项目规则中。
- 目标文件：
  - `AGENTS.md`

## 3. Sequential Thinking 留痕

- 执行时间：2026-04-07
- 结论摘要：
  1. 该要求属于工具生命周期管理规范，最合适写入 `AGENTS.md` 的 `4.1 Serena MCP` 节。
  2. 规则需要可执行，至少覆盖“仅在需要时启用”“用完必须关闭”“不得长期保留多余后台实例”。
  3. 本次以最小写集处理，避免再次把项目规则改散。

## 4. 任务拆分与验收标准

### 4.1 原子任务

1. 规则执行任务
   - 目标：
     - 在 `AGENTS.md` 中补充 Serena 生命周期管理要求
   - 主要写集：
     - `AGENTS.md`

2. 独立验证任务
   - 目标：
     - 复核规则是否准确写入 `AGENTS.md`
     - 复核位置是否合理、措辞是否可执行

### 4.2 验收标准

1. 规则明确要求：仅在确有需要时启用 Serena。
2. 规则明确要求：用完或进入不再依赖 Serena 的阶段时，必须主动关闭 Serena 相关后台进程。
3. 规则明确要求：不得长期保留多余 Serena 实例。

## 5. 执行与验证摘要

- 执行子 agent：`Locke`
  - 结果：
    1. 在 `AGENTS.md` 的 `4.1 Serena MCP` 节补入 Serena 生命周期管理规则。
    2. 规则覆盖“仅在确有需要时启用”“用完或不再需要时必须主动关闭”“不得长期保留多余实例”三项要求。

- 独立验证子 agent：`Dalton`
  - 结果：
    1. 复核确认规则位于 `4.1 Serena MCP` 节，位置合理。
    2. 复核确认文案为中文，且措辞可执行。
    3. 复核确认 `evidence/` 留痕完整。

## 6. 最终结论

- 已将“Serena 用完必须主动关闭、不得长期保留多余实例”的要求写入项目全局规则。
- 该要求现位于 `AGENTS.md` 的 `4.1 Serena MCP` 节，后续可作为默认执行规范。
