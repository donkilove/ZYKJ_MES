# 任务日志：维修下拉与多行原因改造

- 日期：2026-05-15
- 执行人：Codex
- 当前状态：已完成，完整后端目标集存在环境超时

## 1. 输入来源

- 用户指令：将 SCGLXT3 的不良现象/原因固定目录迁入当前维修体系，送修和维修完成不再使用自由文本，并支持多行原因及报废补充联动。
- 代码范围：生产维修后端服务、生产送修弹窗、维修完成弹窗、相关 widget 测试与生产模块集成测试。

## 2. 范围与目标

- 目标：送修现象改固定下拉；维修完成原因改“一级分类 + 二级原因”级联下拉；同一现象允许多原因行；报废已补充仅在存在报废行时可勾选；后端防御校验现象数量与报废补充状态。
- 非目标：不新增数据库字段，不迁移历史自由文本数据。
- 影响范围：`backend/app/services/production_repair_service.py`、`frontend/lib/features/production/presentation/widgets/*repair*dialog.dart`、相关测试。

## 3. 执行记录

- 新增前端不良目录常量 `frontend/lib/features/production/models/defect_catalog.dart`。
- 送修弹窗保留多行能力，将不良现象 TextField 替换为目录下拉。
- 维修完成弹窗支持多行原因、不良现象选择、原因分类/原因级联、按现象数量聚合校验、报废补充联动禁用。
- 后端维修完成增加按送修明细聚合校验，并拒绝无报废原因行时提交 `scrap_replenished=true`。
- 测试补齐送修下拉、多行现象、维修原因多行与报废补充联动断言。
- Graphify：读取 `graphify-out/GRAPH_REPORT.md` 作为结构导航；未刷新图谱。

## 4. 验证与交付

- `C:\flutter\bin\dart.bat format ...`：通过。
- `C:\flutter\bin\flutter.bat test test/widgets/production_order_query_page_test.dart test/widgets/production_repair_scrap_pages_test.dart`：通过，40 项通过。
- `$env:PYTHONPATH='backend'; pytest backend/tests/test_production_module_integration.py::ProductionModuleIntegrationTest::test_complete_repair_order_accepts_multiple_causes_for_same_phenomenon -vv -s`：通过。
- `$env:PYTHONPATH='backend'; pytest backend/tests/test_production_module_integration.py::ProductionModuleIntegrationTest::test_complete_repair_order_rejects_cause_phenomenon_quantity_mismatch backend/tests/test_production_module_integration.py::ProductionModuleIntegrationTest::test_complete_repair_order_rejects_scrap_replenished_without_scrap -vv -s`：通过。
- `python -m compileall backend/app/services/production_repair_service.py backend/tests/test_production_module_integration.py`：通过。
- `git diff --check`：通过，仅有既有 LF/CRLF 提示。

## 5. 未完成验证

- `$env:PYTHONPATH='backend'; pytest backend/tests/test_production_module_integration.py backend/tests/test_quality_module_integration.py -q`：15 分钟超时未完成。
- 质量模块单个维修完成契约用例也在 3 分钟超时；后端新增窄范围用例已通过，但完整集成回归需在更适合的测试数据库环境继续跑。
