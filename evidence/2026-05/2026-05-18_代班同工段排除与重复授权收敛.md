# 任务日志：代班同工段排除与重复授权收敛

- 日期：2026-05-18
- 执行人：Codex
- 当前状态：已完成

## 1. 输入来源
- 用户指令：代班页面下拉框排除本工段人员；同一用户可将同一订单同时代班给多人，但同一代班人重复授权时需拦截报错。
- 需求基线：`AGENTS.md`、`docs/AGENTS/*.md`
- 代码范围：`backend/app/services/assist_authorization_service.py`、`backend/app/core/user_facing_errors.py`、`frontend/lib/features/production/presentation/widgets/production_apply_assist_dialog.dart`、相关测试

## 2. 范围与目标
- 目标：统一前后端代班发起口径，屏蔽当前工段操作员代班，允许同订单同目标多代班人并行授权，仅拦截同代班人重复授权。
- 影响范围：生产模块代班创建服务、前端代班弹窗、用户可见错误文案、代班相关回归测试。

## 3. 执行记录
- 主要改动：
  - 后端创建代班时新增当前工段 `operator` 保护，并将重复授权判定收窄到同 `helper_user_id`。
  - 前端代班弹窗过滤当前工段操作员与工段选项；无候选时自动切换角色或禁用提交并展示提示。
  - 补齐前后端错误映射“本工段操作员无需发起代班”。
  - 新增/调整代班相关后端与 Flutter 测试。
- 项目结构记忆：读取了 `graphify-out/GRAPH_REPORT.md` / `graph.json` 作为结构导航，未刷新图谱。
- 工具降级：
  - 后端 pytest 需显式设置 `PYTHONPATH=backend`。
  - Flutter 不在 PATH，改用 `C:/flutter/bin/flutter.bat`。

## 4. 失败重试记录
| 轮次 | 失败现象 | 根因判断 | 修复动作 | 复检结果 |
| --- | --- | --- | --- | --- |
| 1 | `pytest` 找不到 `app` 模块 | 当前命令未带 `PYTHONPATH` | 改用 `PYTHONPATH=backend` 运行 | 通过 |
| 2 | `flutter` 命令不存在 | 本机 PATH 未注入 Flutter SDK | 改用 `C:/flutter/bin/flutter.bat` | 通过 |
| 3 | 后端 assist 集成测试新增用例失败 | 测试隔离层导致旧 token/直查 DB 断言不稳；helper 缺少首件权限 | 改为重新登录取 token、接口列表断言，并补齐 helper 权限与首件前置 | 通过 |

## 5. 验证与交付
- 验证命令：
  - `PYTHONPATH=backend python -m pytest backend/tests/test_production_module_integration.py -k assist`：通过
  - `PYTHONPATH=backend python -m pytest backend/tests/test_transaction_boundary_unit.py -k assist_authorization`：通过
  - `C:/flutter/bin/flutter.bat test test/widgets/production_order_query_page_test.dart`：通过
  - `C:/flutter/bin/flutter.bat test test/services/production_service_test.dart`：失败，存在与本次改动无关的既有基线问题（500 错误文案与 422 字段文案期望不一致）
- 未验证内容：未执行更大范围 `integration_test`；本次只做代班相关定向回归。
- 残余风险：`production_service_test.dart` 当前基线已有 3 个非本次引入的失败，后续若要做全量前端服务回归需先收敛这些既有断言。
- 是否满足任务目标：是
- 迁移说明：无迁移
