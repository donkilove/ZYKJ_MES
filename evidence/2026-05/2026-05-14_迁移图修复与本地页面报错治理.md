# 任务日志：迁移图修复与本地页面报错治理

- 日期：2026-05-14
- 执行人：Codex
- 当前状态：进行中

## 1. 输入来源
- 用户指令：按仓库级根修复方案，修 Alembic 迁移图、恢复本地数据库升级链并补测试。
- 需求基线：`AGENTS.md`、`docs/AGENTS/*.md`
- 代码范围：`backend/alembic/versions/`、`backend/app/bootstrap/`、`backend/tests/`

## 2. 范围与目标
- 目标：消除 Alembic 迁移图重复 revision 与循环问题，恢复 `mes_process` 缺列升级路径，并把错误前移到启动期。
- 影响范围：后端启动 bootstrap、Alembic 迁移图、启动期单测、迁移图健康检查。

## 3. 执行记录
- 主要改动：修复多设备工序迁移 revision 冲突；为 bootstrap 增加 Alembic 图校验；补 Alembic 图健康测试与 bootstrap 异常回归测试。
- 项目结构记忆：读取 `graphify-out/GRAPH_REPORT.md` 辅助定位模块。

## 4. 验证与交付
- 验证命令：
  - `cd backend && python -m alembic -c alembic.ini heads`
  - `cd backend && python -m alembic -c alembic.ini history`
  - `cd backend && python -m alembic -c alembic.ini upgrade heads`
  - `cd backend && python -m pytest tests/test_startup_bootstrap_unit.py tests/test_alembic_graph_health.py`
  - 只读服务烟测：`list_stages`、`list_craft_processes`、`list_maintenance_plans`、`list_work_orders`、`list_my_orders`
- 验证结果：
  - Alembic 单 head 恢复为 `e6f7a8b9c0d1`
  - `alembic history` 不再报 `CycleDetected`
  - 本地数据库已升级，`mes_process` 已存在 `first_article_check_content`、`first_article_test_value`、`allow_multi_device_production`
  - `pytest` 共 5 项通过
  - 服务层烟测恢复可读，不再触发 `UndefinedColumn`
- 是否满足任务目标：是
